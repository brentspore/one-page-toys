---
name: Cross-device handoff
description: Resume-here state + how-to-continue, kept IN the repo so it travels via git pull between machines
type: project
---

# Handoff — resume here

This doc lives in the repo (`.ai/memory/`), so it syncs between devices via `git pull`.
It is the authoritative current-state bridge. (A richer running log lives in the *global*
`~/.ai/memory/PULSE.md`, but that is machine-local and may NOT exist on every device — trust
this file when they disagree, and note `project.md` here is from the pre-2026-06 era and is stale.)

**This is a handoff, not a journal.** Keep ONE current-state block below and overwrite it each session; the full per-toy build history through 2026-07-07 (toys 001–069, every lesson and convention as it was learned) is preserved verbatim in [archive/HANDOFF-archive-2026-07-08.md](archive/HANDOFF-archive-2026-07-08.md).

## Current state

**Last updated:** 2026-07-21 · **Latest pushed site commit:** `main c28822a` · **88 toys live** at onepagetoys.com.

- **SHIPPED in `main c28822a` (2026-07-21): Shuriken Night combat-feedback pass + a shared More Games alignment fix.** Owner feedback after playing: oncoming ninjas needed to look better and animate, a thrown blade had to cost a life like a melee hit does, and every hit needed a strike animation — small for hits 1-2, a **bigger weapon-specific finish** on the 3rd before the end screen. **(1) Melee no longer teleports damage:** a ninja reaching `ATTACK_RANGE 2.9` now enters an `attack` state and **winds up visibly for `ATTACK_WINDUP 0.44s`** (lunging in, blade cocked overhead) before the blow lands — a real tell you can still kill through, replacing the old instant despawn-and-damage. **(2) Thrown blades:** the kunai/player test is now a **swept 4-step check against a 0.7m cylinder** — the old single-point-per-frame test could step straight over the hit radius on a slow frame, so blades sometimes passed through you harmlessly. **(3) `hurtPlayer(kind, srcX)`** takes `"melee"` or `"blade"` and drives a `strike` object; non-fatal = 0.55s, fatal = 2.0s with `dying=true`, `timeScale 0.2`, and **`endGame()` deferred until the animation finishes** (guarded with an early return so it can't fire twice). While `dying`, `update()` returns early after FX so nothing else can land during the finish. **(4) Animations:** melee = a bowed lens-shaped **slash** (glow pass + hot white core, sharper end taper) sweeping from the attacker's side, doubled into a crossing X on the kill, with the **attacker looming up out of the dark** (rim-lit — a plain black fill was invisible against the night sky) and the frame closing in; blade = the kunai **buried in the view**, trembling, with radial cracks spreading and a contact flash. Fatal adds `sndFatal()` (steel draw-and-cut for melee, struck-metal ring for a blade, both over a long low tail). **(5) Ninja animation:** cadence now derives from each type's actual speed (a brute lumbers, a dropper sprints), plus a forward run lean carried through the torso/head/hood, a waist sash, opposed arm swing, and the attack pose. **⚠ Trap hit here:** the attack-pose `else` was written without braces, so it guarded only the first statement and the normal arms drew *on top of* the attack pose — `node --check` passes this happily, so brace-scope bugs need eyes, not a syntax check. **(6) Shared fix — More Games title alignment** (owner: titles indented vs subtitles): `.mg-body` was **inheriting `text-align:center` from the host toy's `.panel`**, so the shorter title centred inside a box sized by the longer subtitle while the subtitle looked left-aligned. `.mg-body` now sets `align-items:flex-start` + `text-align:left` explicitly (the widget's stated rule is to never inherit from its host). Affects **all 18 pages** that mount the widget; `more-games.js?v=9` across them. Verified 0.0px drift on 4 different hosts. **(7) AUDIO REBUILT on owner feedback** ("make a throw sound like a throw, a slash like a slash, a hit like a hit, a death like a death") — the originals were generic filtered-noise sweeps that didn't evoke their objects. Each is now modelled on what physically makes the sound: **THROW** = the signature isn't the whoosh, it's the **blade flutter** — a sawtooth LFO (~58Hz, slowing to 30Hz as the star spins down) amplitude-modulating a band-passed air rush that sweeps up then falls away (doppler); that chop is what reads as "shuriken" instead of "someone waved a stick". **SLASH** (new `sndSlash(pan, big)`) = a much faster, higher-Q sweep peaking in ~70ms (the swing passing you) plus the **edge singing** after the cut (inharmonic 1 : 2.41 : 4.17); fires on every melee hit and, in its `big` form, as the melee finisher. **HIT** = a mid **contact slap** (the layer that actually reads as impact) over a deliberately **very short (<80ms) low body** — per the Pool lesson, stretch that low sine out and the ear stops hearing an impact and starts hearing a bass note; headshots add a high-passed crack. **HURT** (a blow landing on you) = thick lowpassed body blow + sub punch + the whole bus muffling and swimming back. **DEATH** = the weapon first (big slash, or a buried-blade crack with tremolo'd metal shiver), then a **temple gong death knell** — a bronze inharmonic cluster (1 : 1.68 : 2.35 : 3.11 : 4.42 : 5.9) that pitch-settles as it rings over ~5s, with a mallet strike and a sub falling to nothing. The gong is what says "this is the end" rather than "that hurt", and it suits the setting. **⚠ Caught a duplicate-definition trap:** adding the new `sndFatal` left the OLD one still defined further down the file, and the later declaration silently wins — `node --check` is blind to it. **Levels measured with a temp AnalyserNode on `outGain`** (headless can't hear, but it can measure — same technique as the Pool audio fix): first pass showed the enemy's slash quieter than the player's own throw, so both were raised; final RMS escalates sensibly throw .0099 < slash .0127 < hit .0163 < deflect .0198 < big slash .0266 < hurt .0344 < death .116-.133, nothing clipping. **STILL NEEDS OWNER EARS** — measurement only proves signal exists and is balanced, never that it sounds right. Toy now `script.js?v=3`.

**(8) ⚠ REAL BUG — throwers were completely harmless (owner: "an attacker just standing there throwing at me, I'm not getting hurt").** `throwKunai` fired along the **straight line** to a point at chest height and added a token `+0.6` of lift, but never compensated for gravity over the flight. Drop scales with the square of flight time, so past ~11m it exceeded the lift and every blade sailed **under the player's feet** — and throwers stop at **9-14m**, i.e. exactly the band where 100% of their throws missed. Simulated: 8m arrives y=0.43 (hit), 12m y=-0.14, 14m y=-0.54 (both under the hit window's -0.08 floor). Fix = solve the **actual ballistic arc**: `T = d / KUNAI_SPEED`, `vy = (targetY - startY)/T + 0.5*G*T`, which lands dead on chest height (1.37m) at ANY distance — verified 6m through 16m all arriving at exactly 1.370. Gravity is now a shared `KUNAI_G` constant used by both the launcher and the integrator so they can never drift apart again (they had). **Rebalanced after the fix**, because going from harmless to lethal overshot: one thrower killed a passive player in 9s. `KUNAI_SPEED` 13.5→11 (blade is airborne ~1.06s from 12m = a real window to see and deflect it), fire interval 2.3→3.1s, and a new **`THROW_TELL` 0.42s wind-up** — the arm cocks first and the blade only leaves on release, matching the melee attack's fairness rule instead of a blade simply materialising. Throwers also now **creep forward** to a 6.5m floor rather than parking forever. One thrower now takes ~12s to kill a passive player. **⚠ Test-harness lesson (cost two false failures):** when a damage test runs several cases in one session, life carries over and the game ends mid-suite — later cases then run against a dead game (`update()` returns early while `over`, so projectiles never even move, and a "still airborne after 5s" reading was really "the world is frozen"). Reload to a fresh game between cases. Toy `script.js?v=4`.

**(9) Second audio + variety round (owner: "hit is just a thud, the kill/slash was still just a ding — do attackers only come from the centre window on Hold the line?").** **⚠ The window question was a REAL BUG I had introduced:** the earlier fairness fix set the static spawn arc to `fov*0.4`, but with only **12 bays (30° apart)** and a **57° lens**, that arc admitted exactly **ONE** bay on every device — so every enemy in Hold the line came through the same centre screen. Fixed by making the wall finer and the lens wider: **N_BAYS 12→24** (15° spacing), **fov base 1.05→1.3 clamped [1.25,1.55]** (57°→72° desktop, 88° portrait), arc `fov*0.45`. Now **5 bays** are in play on every viewport with the outermost at 90% across (still fully on-screen); measured **7 distinct spawn bearings** and **0 off-screen enemies across 1176 samples** at 3 viewports. Lanterns moved to every OTHER bay and to the half-step angle so they hang in the GAPS between screens rather than in front of them. **AUDIO — the "ding" was my own `metalRes` Q.** New rule learned: **a bandpass resonator at Q 24-34 rings so narrowly it IS a sine** — the exact ding it was meant to replace; dropped to **Q 11-16**, which keeps audible noise around the partial the way real struck metal does. `sndDeflect`'s pure sine partials converted to `metalRes` too (it was the one true chime left, peaking at 1676Hz). **HIT** gained a **steel bite** (short bright high-passed click = "something sharp landed") plus two resonators for the star shivering where it stuck, over the existing slap + short low body. **SLASH** gained a fast **downward resonant tear** behind the whoosh — that's the layer that reads as something being *cut* rather than air moving — plus a heavy low cleave on the big version. **DEATH gong rebuilt properly:** a bell is a few clean partials (a ding); a gong is a low bronze body under a **dense unstable inharmonic cluster** plus a **shimmer that BLOOMS IN ~0.7s after the strike** as energy spills upward through the plate. Now 3 low sines (58Hz base, reduced so they support rather than dominate) + 8 detuned `metalRes` partials + a swelling filtered noise wash + mallet, ~7s decay. **⚠ Measurement lesson — my first metric was wrong:** spectral flatness over 0-12kHz flagged the *hurt* sound as "tonal" purely because it is dark (all energy low), conflating "dark" with "pitched"; switching to **loudest-bin share + peak frequency** showed the truth — hurt was fine, and the real offenders (hit, slash-big, fatal) were dominated by a bin at **59Hz**, which for an impact/gong fundamental is CORRECT, not a defect. Use peak-bin frequency, not broadband flatness, to judge "is this a ding". Final: nothing clips, no mid/high pure tones remain, levels escalate throw < hit < deflect < slash < hurt < big slash < death. **STILL NEEDS OWNER EARS.** Toy `script.js?v=5`.

**Verified:** life drains exactly `[3,2,1,0]` with no double-counting, end screen held back through the finish then arrives, blade path proven separately (a forced kunai costs exactly one life and registers `kind:"blade"`), death + restart resets cleanly in both normal and reduced motion, and the full play-test suite still passes at 3 viewports × 2 stances. Toy `script.js?v=2`. Temp hooks grep-clean.

- **SHIPPED `main c28822a` (2026-07-21): NEW TOY 088 Shuriken Night** — `toys/shuriken-night/`, category `game`: a **first-person ninja star game** in a moonlit Japanese castle courtyard (owner's Shinobi-inspired backlog idea, escalated to first-person on his call). **Rendering is vanilla Canvas-2D pseudo-3D, no WebGL:** the camera sits at the world origin and only yaws, so the wall/roofline/hills/sky are all cylinders centred on the camera — and **a cylinder at fixed radius projects to a horizontal band of constant height**, which is why the whole backdrop is drawn as bands and angle-profiles instead of geometry. Everything that moves is a real 3D point projected per frame and depth-sorted. Radial ground joints project to *vertical* lines (constant bearing), so no convergence maths. **TWO STANCES, picked at start** (owner asked for both): **Full circle** = free 360° yaw, enemies flank you, off-screen threats show as edge chevrons; **Hold the line** = locked forward. **All four skill layers** the owner picked: throw, **deflect** incoming kunai by hitting them mid-air (+75), **charged fan of three** (hold 0.42s), **focus** slow-mo (meter fills on kills), **ninja magic** screen-clear (2 charges, +1 every 3 waves). 4 enemy types (runner / thrower / 2-hit brute with a red sash / rooftop dropper); the signature beat is a **silhouette appearing behind a lit paper screen, then bursting through it** in a shower of paper shards. Combo chain multiplier, headshots ×1.6, 3 health pips, waves scale, best in `shuriken_best`. **⚠ REAL BUG FOUND AND FIXED IN TESTING: in Hold-the-line, enemies spawned outside the field of view** (spawn arc was a hardcoded 1.15 rad vs a ~0.6 rad half-FOV) — since you cannot turn in that stance, they walked up and hit you with zero counterplay. Spawn arc is now FOV-derived (`staticArc() = fov*0.4`); verified 0 off-screen enemies across 1029 samples at 3 viewports. **⚠ Verification lesson:** an early aim test reported a 0% hit rate and a later one 52% — **both were the test, not the game.** The first aimed at stale coordinates (some literally off-screen at x=2231); the second judged hit/miss *before the star had finished its flight*. Measured properly (fresh in-page aim, full 850ms flight) it is **100% at every distance**. Don't trust a hit-rate harness that doesn't wait out the projectile. **Art took several passes to clear the bar** — first draft was rejected as a void: lanterns rendered as giant grey ellipses (uncapped `p.s` scaling), the roof projected *above* the viewport, and the wall was the same value as the sky so it vanished. Fixes: wall pulled to R=18 and WALL_H=4.7 so the whole silhouette fits the frame, wall darkened to read against a *brighter* horizon, screens lit by default (`baseLit`, seeded at build time or the menu shows dead paper), lantern size **capped in px**, ground given flagstone courses + gravel + moon pooling. The ninja silhouette was rebuilt to human proportions (head 1/7.5 height, shoulders 0.25h) with a knee bend and a blade; **the rim light is an offset copy of the whole silhouette drawn behind the dark body** — robust for any pose, but the offset MUST be clamped to a few px absolute (scaling it with height made a close ninja look like plate armour where the copy showed through limb gaps). Damage flash was also cut hard (was blanketing the screen 0.45s at the exact moment you need to see). **Audio** (compressor + convolver courtyard reverb + master lowpass bus, iOS unlock, sound toggle, one shared noise buffer since per-throw allocation churned the heap): layered throw whoosh + metallic spin, soft low impact, **inharmonic 1:2.76:5.4 steel ting on a deflect**, paper-tear + wood-crack on a screen burst, koto-flavoured pentatonic run on wave clear, low boom + rising shimmer for magic, and a night bed of wind, panned crickets and a distant temple bell. **⚠ NEEDS OWNER EARS** — headless only proves the graph builds. Full pipeline done: registry prepend (13 tags incl. NL key), sitemap (90 urls), `TYPE_NL_PHRASES.shuriken-night`, card rule + `:not()` chain, og-gen No. 088, **real rendered card** (cropped tight on a ninja at 6.3m with a screen bursting behind) + OG, hub bumped `main.js?v=83`/`styles.css?v=91` across all 8 hub pages. Badges docked bottom-right (they sit in the aiming field otherwise — the Nova Coil trap). Verified: 3 viewports × 2 stances 0 console errors / 0 overflow, panel fits at 568×320, search finds it on ninja/shuriken/ninja star/first person/throwing stars/shinobi, card+OG decode, iOS tap-highlight transparent, reduced-motion clean, `shuriken_best` earns tickets, and a **92-page site sweep with 0 errors**. Debug hooks grep-clean.

- **SHIPPED in `main c28822a` (2026-07-21): Ticket system Phase 1 — the silent earning engine.** Owner asked for a site-wide arcade-ticket economy: earn tickets from playing + scoring across all toys, spend them in a virtual store, share what you bought; localStorage-only, user-editable, "just for fun so I'm not concerned about security"; **dark-launched until he opens it to everyone.** Owner chose **vanity collectibles + gag/novelty items only** for the store — deliberately NO per-toy cosmetic unlocks, which is why **no toy's own code was edited** and there is no Phase 3. **New file `assets/tickets.js`** (~470 lines, ES5 IIFE in the tip-jar.js house style, self-contained styles). **Two earn sources, both zero-per-toy-work:** (1) **active time** — 5s heartbeat, accrues only while `visibilityState==='visible'` AND input within `IDLE_MS` 25s, 1 ticket/60s, `DAILY_CAP_PER_TOY` 25/toy/day, plus a `DISCOVERY_BONUS` 15 the first time you ever touch a given toy (an explore incentive across 87 toys); (2) **achievements** — wraps `Storage.prototype.setItem` and watches the score keys the toys ALREADY write. ⚠ **The hook calls the original setItem FIRST and unmodified, then observes inside try/catch**, so a bug in tickets.js can never stop a toy saving; `this===window.localStorage` check keeps sessionStorage clean; a `writing` flag prevents re-entrancy on our own ledger writes. **The `RULES` table is hand-built from a survey of all 30 storing toys and encodes direction semantics** — `up` (nova_best, slice_best, pc_best, stack_best, snake:best, opt-echo-best…), `down` = time/strokes (mines_best_*, maze_best_*, sb_best_*, trailfeed_best_*, pool_best, golf_day_*), `count` = tallies (golf_aces, dice_streak, sb_solves, trailfeed_solves). **Deliberately excluded: `bj_bank`** (a bankroll legitimately goes down, so it is not an achievement) and all settings/sound/diff/seen/theme keys. Awards are **log-shaped** (`BEST_BASE 6 + 5·log2(1+rel·4)`, cap 30) so a 100,000-point arcade toy and a 3-stroke golf hole stay comparable — absolute deltas would let big-number toys dominate. **`seed()` records every pre-existing tracked key at first load WITHOUT awarding**, so a veteran with months of bests starts at 0 rather than getting a payout for history. **Dark launch:** always earns, renders no UI unless `opt_tickets_ui==='1'`; `?tickets=1` turns it on for that device, `?tickets=0` off; going public = flip `UI_DEFAULT` to true + add a hub link. UI = a small bottom-left pill + debug panel (balance/breakdown/recent awards/reset) — **bottom-LEFT deliberately, because the tip-jar and fullscreen badges own the right edge** (verified no overlap; this is the Nova Coil/Dot Loop trap). **Public API for Phase 2:** `window.OPT_TICKETS.balance/lifetime/ledger/award/spend/owned/own/on/showUI/reset`. **Include sweep:** added to **all 95 pages** that load tip-jar.js, inserted **BEFORE each page's own `script.js`** so the hook installs first; done with a script (`scratchpad/add-tickets.cjs`), archive untouched. 3 pages correctly skipped: `scripts/og-gen.html` + `scripts/og-cat.html` (internal render templates) and **`all-toys.html`** (legacy leftover — not in sitemap, not linked from anywhere; `all-toys/` the directory is the live one). **Verified headless, 45 assertions all passing:** 10 toys × 2 viewports 0 console errors / 0 overflow; the safety test (untracked + tracked keys round-trip unchanged, setItem still returns undefined, sessionStorage unaffected); score detection on 6 toys with correct direction (improvement pays, regression pays nothing); non-score keys earn nothing; seeding silent; discovery bonus + time accrual + idle window + hidden-tab = 0; dark-launch gating both ways; badge clears tip jar/fullscreen/play area at 375px; **storage-disabled (private mode) throws nothing**; spend/own API. Then a **full sweep of all 91 live pages: 0 console errors, tickets.js 200 + API present on every one.** Screenshotted on a dark toy, a light tool, mobile, and the hub. **Live-verified on the apex:** toy/card/OG/tickets.js/more-games.js all 200; deployed script contains `N_BAYS = 24`, `metalRes`, `THROW_TELL`, `ATTACK_WINDUP`, `KUNAI_G` and the gong; registry 88 with shuriken-night newest; hub on `main.js?v=83`/`styles.css?v=91`; live play-test at 1280 + 375 in both stances with 0 console errors; tickets silent by default and revealed by `?tickets=1`; search 'ninja star' finds the toy; More Games title drift 0px. **Next:** owner testing via `?tickets=1`, then economy tuning (knobs are the consts at the top of `assets/tickets.js`), then Phase 2 (store page + trophy shelf + gag items + share loop) — both logged in `.ai/memory/BACKLOG.md`.

- **SHIPPED `main 33c4f0c` (2026-07-17): Dot Loop badge relocation** (owner: "Dot loop needs to move the tip jar down like nova coil"). Ported the reusable nova-coil badge-relocation pattern to `toys/dot-loop/`: page-scoped doubled-class CSS override (`.opt-tipjar.opt-tipjar` bottom `max(14px, safe-area-inset-bottom)`, `.opt-fs.opt-fs` +54px above, `transform:none`, `.tj-nudge` animation silenced) docks the shared tip-jar + fullscreen badges in the bottom-right corner out of the drag field, and the boot IIFE in `script.js` plays the center→corner entrance slide (skipped under reduced-motion). `styles.css?v=5`/`script.js?v=3`. Verified headless 1280+375: badges land bottom-right, slide animates then settles to `transform:none`, 0 overflow, 0 toy-code console errors. (Note: `main b31cb8a` "Let Spelling Blocks be the featured partner, everywhere" landed between 3c7a068 and this without a handoff bump — pushed outside a handoff-updating session.)

- **SHIPPED `main 3c7a068` (2026-07-16): Spelling Blocks recolored brown → cool slate** (owner: "I don't like the BG color on spelling blocks. It's too brown looking"). The warm tan/walnut palette that mirrored spellingblocks.com read too brown in BOTH themes; owner picked **cool slate** from 3 options (slate / warm-neutral greige / green felt). **⚠ TWO causes, not one:** (1) the palette itself, and (2) `html, body` painted a `rgba(255,255,255,0.55)` sheen radial in **both** themes — on the dark walnut that white wash lifted it into muddy milk-chocolate. Fix = a **`--sheen` token** (light `0.55`, dark `0.05`) + deleted the now-redundant `:root[data-theme=dark] body::after` that was double-dosing the lift. **Palette:** light `--tabletop:#eceef1` / `--paper:#f5f6f8` / `--ink:#1c2027`; dark `--tabletop:#1b1e24` / `--paper:#242830` / `--ink:#eceef1`; grain `feColorMatrix` RGB recolored brown (.149,.133,.106) → cool neutral (.110,.125,.153); `--blk-text` default + `.blk.is-butter` text + the confetti cream swatch in `script.js` all moved off `#f1e7d0`/`#26221b`. **Brand red accent kept** (`#c7402d` light / `#e5624a` dark) — it still reads on slate and it's the spellingblocks.com identity. **Downstream assets that were keyed to cream (all regenerated):** the card (`assets/cards/spelling-blocks.png`) re-rendered from the live toy showing the **finished CANDLE/POCKET wall** — drove the real UI (Easy tier, click New until the tray matches, type the words, Enter) since internals are closure-scoped; **puzzle 51 in `data.js` is the CANDLE/POCKET set** (`{"b":"DEECKPTLNOCA","p":2,"s":["CANDLE","POCKET"]}`) — handy for future captures. Card CSS backdrop `#ecdfc0`→`#eceef1`. OG regenerated; **og-gen's `.og.ink` light mode moved off warm walnut `#2b2113` → neutral `#23272e`** so `light:true` reads on ANY light hue, not just cream (keeps it reusable). **⚠ Fixed latent drift:** the 6 category pages were still on `assets/styles.css?v=87` while the 2 hub pages were on `v=89` — bumped **all 8 to `v=90`** uniformly. Toy `styles.css?v=2`/`script.js?v=2` (`data.js` unchanged at v=1). Verified light/dark × 375/1280: 0 console errors, 0 overflow, tap-to-builder works, intro + solved overlays + gallery card + OG all eyeballed. Live-verified on apex: 200, deployed CSS has slate + `--sheen`, no `#ecdfc0`/`#201c16` remain, hub on v=90, card/OG byte-match local, 0 console errors both themes. **Open (owner call):** card + OG are on the **light** slate (as before) — could re-render on the dark blue-charcoal if that reads better in the gallery.

- **SHIPPED `main f077df0` (2026-07-16): Spelling Blocks OG text-contrast fix.** The Spelling Blocks OG (No. 087) is the **only** share image with a light (cream) background, but `scripts/og-gen.html` paints every text layer white (`.desc` was `rgba(255,255,255,.74)`, title inherits `#fff`) — built for the dark bg every other toy uses — so on cream the title/description/footer washed out. Fix = a **reusable dark-ink mode** in the template: a `.og.ink` block repaints the chrome in warm walnut `#2b2113` (title via `.og.ink` color, plus `.wm`/`.wm__mark`/`.corner`/`.desc`/`.foot` overrides) and flips the grain to `mix-blend-mode:multiply`; it's opted into **per-entry** with a new `light:true` flag that `render()` toggles as a class. Set `light:true` on the `spelling-blocks` entry + darkened its accent `#c7402d`→`#a8321f` so the "GAME · WORD" eyebrow reads on cream. Regenerated `assets/og/spelling-blocks.png` in place (Playwright → `file://…/scripts/og-gen.html#spelling-blocks`, 1200×630 @2x = 2400×1260, 0 console errors). Live-verified on apex: the deployed PNG matches the new file byte-for-byte (1,715,569 B). **Reusable:** any future light-background toy just sets `light:true`. Only `scripts/og-gen.html` + the one PNG changed; no hub/registry/cache-bust touched (OG images aren't versioned).

- **SHIPPED `main 44369e4` (2026-07-15): Spelling Blocks feeder (No. 087)** — `toys/spelling-blocks/`, category `game`: a random-puzzle PRACTICE edition of the daily word game at **spellingblocks.com** (a traffic feeder, same pattern as the Trail Game). **Game:** you get 12 lettered toy blocks and sort them ALL into real English words — no leftovers — finishing at/under **par** (the target word count). Tap a tray block → it joins the builder; tap a builder block to return it; **Place word** (min 3 letters, must be a real word) locks it onto the **wall** (courses stack up); tap a placed course to break it back to the tray. Shuffle, Clear, **Hint** (peek — pulses the blocks of a makeable word, preferring an intended-solution word), full keyboard (type / Backspace / Enter). Win = all 12 placed; **clean** = words ≤ par, **under par** = fewer. **Difficulty maps to par:** Easy = 2 words, Medium = 3, Hard = 4 (66/51/15 puzzles). **⚠ Dictionary approach (reusable):** the React app imports `an-array-of-english-words` (275k words) — too big to ship. Instead a build-time script (`scratchpad/gen-puzzles.cjs` + `gen-data.cjs`, run against spelling-blocks' node_modules) takes the repo's **curated solvable letter sets** (DAILY+PRACTICE solutions, each partitions cleanly into real words = guaranteed solvable), deduped by sorted-letter key, and computes the **union of every dictionary word (3-9 letters) formable from any puzzle's 12 letters** → `data.js` (`window.SB_DATA`, 31,884 words + 132 puzzles, 279KB raw / ~80KB gz). **Key insight:** any word the player can build from a tray is by construction a multiset-subset of that puzzle's letters, so it's in the union — **validation is one `WORDSET.has(word)` lookup**, no per-puzzle lists, no runtime dictionary scan (except the on-demand Hint). **Look mirrors spellingblocks.com** (ported `.sb-block` bevel): warm **paper tabletop** (light) / **walnut** (dark) via `opt-theme`, physical beveled blocks in 4 toy colors (cobalt/grass/butter/cherry), Bricolage Grotesque + Archivo Black + Schibsted Grotesk (Google Fonts). Recessed slot surfaces (dashed tray, inset builder + wall). **Layout fix (verified):** stage centers the tray+builder+wall as one compact group (`justify-content:center`, wall `flex:0 1 auto; min-height:104px; max-height:44vh`) — an earlier `flex:1` wall ballooned into a void on tall/desktop viewports. **Audio bus** (compressor + convolver reverb + master lowpass, iOS 1-sample unlock, `sb_sound` toggle): layered wooden-block **clack** (noise tick + woody triangle body, pitch rises as the word grows) / firmer double-knock **place** with room / light **tick** on remove/break / soft **invalid** thud / pentatonic **win** arpeggio bloom / hint shimmer. **⚠ NEEDS OWNER EARS** (headless can't audition). **Feeder funnel** (same as Trail): persistent daily-CTA banner + solved-screen CTA (both → spellingblocks.com with `utm_source=onepagetoys` + GA `outbound_click`), **live countdown to next local midnight**, colored-emoji-wall **share** loop, first-visit **how-to intro** (`?` re-opens), **streak nudge** escalating the CTA after ≥3 solves (`sb_solves`), best-time per difficulty (`sb_best_<diff>`), **confetti**. Full pipeline: registry prepend (11 tags incl. NL-key `spelling-blocks`), sitemap (89 urls), `TYPE_NL_PHRASES.spelling-blocks` (anagram/scramble/word builder/spellingblocks…), card CSS + `:not` chain, og-gen No. 087 (`GAME · WORD`), rendered card (**CANDLE/POCKET** colorful blocks on paper) + OG. Toy `script.js?v=1`/`styles.css?v=1`/`data.js?v=1`, hub `main.js?v=81`/`styles.css?v=89`. **Cross-promo:** added Spelling Blocks to `assets/more-games.js` (WORD-block data-URI icon, `slug:"spelling-blocks"` → the feeder), `more-games.js?v=8` across 17 toy pages. Live-verified (0 console errors mobile+desktop, full solve Easy/Med/Hard, 4-course wall fits @720h, 0 overflow @375, both themes, hint/shuffle/clear/break, gallery card + search "spelling" = 1/87, icon data-URI decodes). **⚠ OPEN: (1) owner audio-by-ear pass; (2) sibling-repo cross-promo** — `five-second-game` + `the-trail-game` `MoreGames.tsx` NOT edited (pushing them auto-deploys production 5secondgame.com / thetrailgame.com — gated on explicit owner OK, per the cross-promo deploy model).
- **SHIPPED `main d1a691c` (2026-07-15): The Trail Game feeder (No. 086)** — `toys/trail-game/`, category `game`: a random-puzzle PRACTICE edition of the daily at thetrailgame.com (a traffic feeder). Puzzle generator **ported verbatim** from the-trail-game repo (`src/lib/game/*`: cyrb53+mulberry32, serpentine + 4000 backbites → random Hamiltonian path, dots in order, walls only on non-solution edges = always solvable), random seed like `generatePuzzleFromSeed`. Vanilla JS + inline **SVG** board, full drag / rewind / dot-order / win logic ported from `Board.tsx`. **Theme mirrors the real game** (warm-paper light / charcoal-red dark, deep-red trail; light+dark via `opt-theme`) + the tear-off dashboard. **Feeder funnel:** persistent daily-CTA banner + solved-screen CTA (both → thetrailgame.com with `utm_source=onepagetoys` + GA `outbound_click`), **live countdown to next local midnight**, share loop, **how-to intro** (first-visit; `?` re-opens), a **streak nudge** escalating the CTA after ≥3 practice solves (`trailfeed_solves`), difficulty picker (Easy/Med/Hard), best-time, audio (pencil tick / dot ping / win chime), **confetti** on solve. Full pipeline (registry, sitemap, NL incl. zip/linkedin-zip, card CSS + `:not`, og-gen No. 086, rendered card [red trail on charcoal] + OG). `script.js?v=2`/`styles.css?v=2`, hub `main.js?v=80`/`styles.css?v=88`. **Cross-promo:** repointed the stale "Wilderness survival" Trail tile in `assets/more-games.js` → the feeder (correct "One-line path puzzle" tagline + grid-trail data-URI icon + `slug:"trail-game"`), `more-games.js?v=7` across 17 toy pages; **five-second-game** `MoreGames.tsx` same repoint (pushed `39ccff6` → 5secondgame.com). **the-trail-game's own list intentionally skipped** (it excludes itself). Live-verified (feeder/card/OG 200 [card had brief Vercel edge lag], registry, more-games v7 + feeder URL, drag-solve wins, both themes, 375px, 0 errors). **⚠ Audio needs owner ears.** Also folded in: Spelling Blocks graduated to its own property (spellingblocks.com). **Follow-up SHIPPED (`38d2819`, live):** the how-to intro step text fragmented (each `<li>` is `display:flex`, so the text nodes + `<b>` tags became separate flex items and wrapped oddly) → wrapped each step's copy in a `.intro__t` span (`flex:1;min-width:0`), `styles.css?v=3`; live-verified.
- **SHIPPED `main 0b5b2a5` + `844db1f` (2026-07-15): Bubble Wrap look + sound polish + HUD-overlap fix** — `toys/bubble-wrap/` is a DOM/CSS grid (not canvas). **Look** (`styles.css`): glossier 3D dome (richer radial gradient + layered inset shadows — ambient occlusion + inner light + inner glow), broad sheen `::before` + tight specular `::after` glint, and the popped state now settles into a dark **deflated wrinkled remnant** (no longer vanishes) via a `bw-pop` squish keyframe (bulge → collapse → flat); reduced-motion disables the anim. **Sound** (`script.js` `popSound`): rebuilt into 3 layers through a `DynamicsCompressor` bus — a crisp high-passed noise **snap** + a resonant pitch-dropping triangle **body** + a low sine air **thunk**, with per-pop jitter so a fast drag never sounds machine-gun. `script.js?v=4`/`styles.css?v=5`. **HUD fix (`844db1f`):** the stats+reset `.hud` shared the top-right height with the `--tr` "Bubble Wrap" frame label and overlapped — moved the HUD to `top: clamp(40px,7vw,58px)` (stacked below the label) and nudged the sheet to `top: clamp(66px,10vh,92px)` so it clears the bubbles; verified no HUD/label/bubble overlap desktop+mobile. Live-verified (200, v=4/v=5, layered-pop + remnant/keyframe deployed, 0 console errors, 0 overflow @375). **⚠ Sound needs owner ears** (headless can't audition).
- **SHIPPED `main 36fedfe` (2026-07-15): Slice It richer fruit rendering** (backlog polish) — rebuilt `drawFruit`/`drawHalfFace` in `toys/slice-it/script.js`: lit-sphere shading (ambient-occlusion rim + warm bounce light + broad sheen + tight specular) + a soft world-space **drop shadow** per fruit; a per-fruit `surfaceDetail()` keyed off a new `kind` field on each TYPE (melon stripes / citrus pores+navel / berry achenes+calyx / stone seam); juicy sliced faces (citrus **radial segments** + pith spokes, watermelon seeds, golden plum flesh + brown **pit**, wet-gloss sweep); glossy juice droplets. `drawFruit(type,r,isHalf)` skips the strawberry calyx on halves. `script.js?v=4`. Live-verified (200, v=4, new code deployed, 0 console errors in showcase + real play). Backlog item marked done. (Same session: the home-count "wrong" report was a non-issue — 85 = 85 real live toys; owner had misread the newest-toy hero.)
- **SHIPPED `main 1473ff5` (2026-07-15): per-category landing pages so category-filtered shares get their own OG image** — `?cat=<cat>` is a JS-only gallery filter, so social crawlers (no JS) only ever saw the default all-toys OG. Fix = **6 static per-category pages** `all-toys/{visual,game,audio,wellness,utility,simulation}/index.html`, generated from `all-toys/index.html` (via a throwaway `/tmp/gencat.js`: rewrote `../assets/`→`../../assets/`, per-cat `<title>`/meta-description/canonical/`og:image=cat-<cat>.png`/twitter/JSON-LD, `<body … data-cat="<cat>">`, custom pagehead H1). `assets/main.js` (bumped **v79**) now reads a `data-cat` body attr as the category fallback (`forcedCategory()`) and **suppresses writing `?cat=` when it matches** so the pretty URL stays clean; the pages pre-filter the gallery for human visitors (verified 21/34/9/7/9/5). Sitemap lists all 6 (priority 0.9). **Live-verified on www:** all 6 pages 200, each serves its own `cat-<cat>.png` og:image + `/all-toys/<cat>/` canonical, deployed `main.js?v=79` has `forcedCategory`. The 6 `cat-*.png` images + reusable `scripts/og-cat.html#<cat>` template (fanned 3-card montage) shipped with the prior commit. **⚠ Reusable:** for static per-filter OG on a JS-filtered gallery, generate thin per-value landing pages with static meta + a `data-<x>` body attr the client reads as a fallback. Backlog: Mandelbrot + the 6 generative visuals marked `done`.
- **SHIPPED `main 29f9d43` (2026-07-15): Mandelbrot (079) + SIX generative-visual toys (080–085) + Trail Game friend + category OG images** — the whole "next generative-visual round," all live-verified (7 toy pages 200, cards/OGs 200, registry updated, hub `main.js?v=78`/`styles.css?v=87`, deployed mandelbrot has `uPower`). **Mandelbrot** (`toys/mandelbrot/`, No. 079, `visual`): raw-WebGL fractal explorer — ONE fragment shader templated into TWO programs, a fast single-precision path + an emulated **double-precision (df64 / Dekker split)** path that auto-engages at css-scale < 1e-5 for deep zoom to ~1e-12 (mag ~1–8 billion, hard-capped so it never pixelates). 5 hand-tuned cyclic palettes, live **Julia morph** (drag sweeps seed c), and a **cinematic auto-journey**: Mandelbrot mode = deep-zoom waypoint dives PLUS a **Multibrot power morph** (`uPower` animates 2→7→2 at the wide "home" beats, float path only); Julia mode = a seed-morph through 6 curated Julia sets. Journey hides all chrome (`body.is-cinematic`) as a screensaver. Ambient open-fifths drone (mid-register so it's audible, NOT sub-bass) + palette cycling + idle drift (10s) = always-alive. ⚠ **KEY FIXES from owner feedback:** (1) "pixels get larger at deep zoom" = renderScale dropped to 0.72 during cycling → now **full-res whenever idle/cycling** (reduced only during active gesture/journey-flight); (2) df engages earlier (threshold 1e-5) so the float→df seam is always crisp; (3) copy softened "infinite … forever" → "zoom deep into … endlessly repeating detail" (true infinite zoom is impossible — honest depth cap). `script.js?v=4`. **SIX generative visuals** (each self-contained, dark full-bleed, always-alive + interactive, own palettes + subtle ambient audio, screensaver-grade — built by 6 PARALLEL subagents, then batch-registered/verified/carded by the parent): **080 Slime Mold** (Physarum ~42k agents, trail diffuse/decay vein networks, tap = food), **081 Particle Life** (species attraction matrix + spatial grid, "New rules" reroll), **082 Flow Field** (perlin field, silky additive ribbons, drag-stir / tap-reseed), **083 Reaction Diffusion** (Gray-Scott + emboss/relief, 5 regimes, drag-seed), **084 Ink Marbling** (authentic area-preserving marbling map + comb, tap-drop / drag-comb, idle auto-drop), **085 Stained Glass** (WebGL 2-nearest Voronoi + leading + moving-sun backlight, tap-pane / drag-herd / Settle = Lloyd). **⚠ ALL 7 NEED OWNER AUDIO-BY-EAR** (headless only proves the graph builds). Also: **The Trail Game added to Friends of the gallery** (badge on both hub pages + `PARTNER_META` `thetrailgame.com` + `assets/partners/the-trail-game.png`); **6 CATEGORY OG images** (`assets/og/cat-{visual,game,audio,wellness,utility,simulation}.png` via new `scripts/og-cat.html#<cat>` — a fanned 3-card montage per category). **⚠ Reusable patterns:** a screensaver toy = continuous rAF loop (`if(!overlayUp && !REDMO && !hidden) requestRender()`) + `visibilitychange` pause + idle drift + palette cycling; the 6-in-parallel workflow = subagents build ONLY `toys/<slug>/` (touch NO shared files), parent does registry/sitemap/main.js-NL/styles.css-card+`:not`/og-gen/cache-bust + card+OG capture serially. Card capture: `waitForTimeout`-based development (NOT per-frame `evaluate(rAF)` — pathologically slow under swiftshader), dsf 2, chrome hidden, `sips` to 1080.

- **SHIPPED `main a4d0ebc` (2026-07-14): favicon rebrand + Minesweeper cross-promo** — two things. (1) **Site favicon redone from the header logo.** The old `assets/favicon-16/32/192.png` + `apple-touch-icon.png` were the **pre-Geist blue/purple blocks** (off-brand leftover). Regenerated all four from the actual header brand mark `.brand::before` = a rounded square split **ink-left / surface-right** with a hairline border (light theme: black-left / white-right / border). ⚠ Key gotcha: a *static* favicon can't theme-switch like the CSS mark, and a pure-**ink** border **vanishes on dark tab bars** (left half + border disappear on `#000`) — so the favicon uses a **neutral grey hairline** (`#9a9a9a`, sw bumped to ~2 at 16px so it still reads) which holds the split-square silhouette on white / grey / black tabs alike. `apple-touch-icon` is **full-bleed white** (iOS masks its own corners; reproduces the light-mode header look). Generated via Playwright native-size screenshot of an inline SVG (`deviceScaleFactor:1`, `omitBackground` for the transparent browser icons) — see the throwaway `gen-favicons.js`; concept picked from a rendered candidate sheet (blocks / play-triangle / split-square — split-square = the actual logo, chosen). **Cache-bust:** icon links had **never** carried a `?v`, so added `?v=2` to the three referenced links (`favicon-16/32`, `apple-touch-icon`) across **all 80 live pages** (else the old icon stays cached). ⚠ The sweep's `grep -v /archive/` did NOT exclude archive on BSD grep (paths came back without `./`, so `/archive/` didn't substring-match) — it touched 58 `/archive/` files; caught in `git status` pre-commit and reverted with `git checkout -- archive/`. **Archive is intentionally left on the old (unversioned) favicon refs** — legacy, don't touch; it still renders the new PNGs since the files are shared. (2) **Minesweeper added to the More Games cross-promo** (`assets/more-games.js`, with `slug` for self-exclusion) with a custom **ember-mine-on-obsidian-glass** data-URI SVG icon (matches the toy skin); `more-games.js?v=6` across the 17 toy pages that load the widget. Verified headless: self-excludes on its own page (0/25), appears in rotation elsewhere, icon loads (only the 3 *remote* favicons — Global War/Trail/5SG — fail in the sandbox, by-design fallback to initial). Live-verified on www: favicon-32?v=2 → 200/667B/PNG 32×32, root serves versioned link, live `more-games.js` has Minesweeper, trio page loads `?v=6`. **OPEN:** the **two sibling repos** (`five-second-game`, `the-trail-game`) have the matching Minesweeper `MoreGames.tsx` entry **staged-but-NOT-pushed** — pushing each auto-deploys a production site (5secondgame.com / thetrailgame.com), so it was gated pending explicit owner OK for those production deploys.
- **SHIPPED `main 1f8bb3c` (2026-07-14): Minesweeper (No. 078)** — the classic mine-clearing logic puzzle, `toys/minesweeper/`, category `game`, live-verified (toy/card/OG 200, registry top, hub `main.js?v=76`, deployed `script.js` has `placeMines`; one transient 404 during Vercel edge rollout, then 6/6 consecutive 200). Full-bleed Canvas 2D, **Obsidian-Glass** skin (owner picked it from 3 dark options: obsidian-glass / neon-grid / slate-brass). **Rules:** first tap always safe (`placeMines(safe)` runs AFTER the first click, banning the clicked cell + its 8 neighbors → adjArr[safe]=0 so the opener always floods); recursive flood-fill BFS with staggered `revealT` for a ripple; classic 1-blue/2-green/3-red number palette brightened for dark (`NUMCOL`). **Input (touch-first):** tap=reveal, **long-press (380ms)=flag** (+haptic), right-click=flag on desktop, **tap a revealed number=chord/sweep** when flags around it match. Pointer handler gates long-press to `pointerType!=='mouse'`; `MOVE_CANCEL` = 0.62·cell drag cancels a tap. **Difficulties** Easy 9×9/10, Medium 16×16/40, Hard 30×16/99 — Hard **transposes to 16×30 on portrait** (orientation chosen at newGame from viewport aspect; square boards unaffected) so tiles stay tappable at 375px. Timer + **best-per-difficulty** in `localStorage` (`mines_best_<diff>`, `mines_diff`). **Juice:** glass-bevel tiles (raised covers / recessed wells), cover-dissolve on reveal, ember-orb mines, exploded-mine halo + spark particles + screen shake + red flash, crossed-out wrong flags, win confetti + gold title; all gated by `prefers-reduced-motion` (headless forces RM=reduce, so board/confetti shots need Playwright `reducedMotion:'no-preference'`; the gradient `background-clip:text` panel title renders as a solid bar in headless — known artifact, real on browsers). **Audio bus** (convolver reverb + compressor + master lowpass, iOS 1-sample unlock, `outGain` sound toggle): soft reveal tick / rising pentatonic shimmer on a flood / flag ticks / low sub-160Hz boom on detonation / pentatonic win chime — **NEEDS OWNER EARS** (headless can't audition). Panel hosts More Games + Share (`#overlay .panel`, stable children so the two shared scripts' appended nodes survive; `window.OPT_SHARE_TEXT` set to a win-time-aware line). Full pipeline: registry prepend (11 tags incl. NL-key `minesweeper`), sitemap, `TYPE_NL_PHRASES.minesweeper` (search hits minesweeper/mines/logic puzzle/deduction), card rule + `:not()` chain in `styles.css`, real rendered `assets/cards/minesweeper.png` (Medium board still) + `assets/og/minesweeper.png` (og-gen `#minesweeper`, No. 078, GAME·PUZZLE), hub cache-bust `main.js?v=76`/`styles.css?v=85`. Temp `window.__ms` verification hook grep-cleaned. **OPEN:** (1) owner audio-by-ear pass; (2) ✅ cross-promo `more-games.js` DONE for OPT in `a4d0ebc` (see top bullet) — sibling-repo pushes still pending owner OK.
- **SHIPPED `main 4c35956` (2026-07-14): dropped last Lovable dependency** — the "Friends of the gallery" **5 Second Game** partner badge (`index.html` + `all-toys/index.html`) and its featured-card icon lookup (`PARTNER_META` key in `assets/main.js`) still pointed at `five-second-game.lovable.app`, which now only resolves via a **Lovable 302 redirect** to `5secondgame.com` (the sibling site is on Vercel now). Repointed all 3 refs directly to `https://5secondgame.com` and rekeyed `PARTNER_META` to host `5secondgame.com` (must match `partnerHost()` output or the featured card loses its icon+blurb). Bumped `main.js?v=75` on both hub pages. Live-verified on www: index serves `?v=75`, badge href is `5secondgame.com`, live `main.js` has the `"5secondgame.com"` key; `grep -ri lovable` over shippable code (excl `/archive/`, `.claude/`) is now clean. NB: the site's own favicon was never a Lovable file — the codebase comment just calls the partner icon a "favicon."
- ⚠⚠ **HOSTING MOVED TO VERCEL (~2026-07-14):** onepagetoys.com is now served by **Vercel**, not GitHub Pages. Push to `main` → Vercel auto-deploys (~1-2 min). The apex **onepagetoys.com 308-redirects to www.onepagetoys.com**, so **live-verify against `https://www.onepagetoys.com/…`** (or `curl -L`) — grepping the bare apex only returns the redirect stub and falsely reads "stale". The GitHub `pages-build-deployment` action still runs but is a **legacy leftover** that does NOT serve the site; ignore its status. (Discovered while verifying the Pool flip fix — a "stale CDN" that was really me curling the redirecting apex. `reference.md` updated.)
- **SHIPPED `main 5770d50` (2026-07-14): Pool orientation fix** — flipping a phone between portrait/landscape re-orients the table but previously left balls at stale coords (off-table). Fix: `resize()` snapshots each ball in **table-normalized coords** (`toNorm`: lengthFrac 0..1 along the long axis + widthFrac ±0.5 across, plus length/width velocity components) BEFORE rebuilding geometry, then `fromNorm` re-lays them onto the new orientation (velocity rotated + rescaled by table-size ratio). Aspect is 2:1 in both orientations so it's a clean rotate+scale; verified flip both ways keeps oob=0, rack minGapR steady at 2.08 (no overlap), 16 balls, 0 errors, live flip smoke on www clean. Also added an `orientationchange` listener (iOS reports stale dims on the immediate resize). `pool script.js?v=5`.

- **SHIPPED `main 2a2f894` (2026-07-14): Pool (No. 077)** — top-down pocket billiards, `toys/pool/`, category `game`, live-verified (page/card/OG 200, registry top w/ dir-path, live break 0 console errors). Real 2D physics: substepped elastic ball-ball + cushion bounce + pocket capture w/ drop anim (verified **no tunneling** on a full break via a temp `__pool` hook that sampled outOfBounds mid-flight; settles ~2.2s at `FRIC 2.2`). **Drag-back aim** from the cue ball (cue stick pulls back + power ring, de-cheated: short guide, no trajectory line). **Spin picker** sets follow/draw/english, transferred to the cue ball on its FIRST object-ball contact. 6 pockets (4 corner + 2 side, side pockets on the long edges). **Both modes via toggle:** solo clear-the-table (fewest shots, `pool_best`) + pass-and-play 8-ball (groups assign on first pot after break, foul = scratch/no-contact → ball-in-hand for opponent, pot the 8 after clearing your group to win / early-8 or scratch-on-8 = loss). Table **orients to viewport** (horizontal desktop / vertical phone) via a length/width-axis mapping (`tp()`/`lenDir()`/`widDir()`); physics runs in screen coords. Felt+lamp cached to an offscreen `tableCanvas`; glossy numbered balls (solids/stripes/8/cue), chalk puff, confetti. **Badges docked bottom-right + center→corner entrance anim** (reusable pattern, same as Nova Coil). **AUDIO iterated 3× on owner ear feedback** (`script.js?v=4`): (1) removed a pitched mid-range sine "beep" from the ball-clack and cue (→ noise-only crack/tock); (2) owner said "really faint" → **measured peaks via a temp analyser** and found cushion/pot near-silent (0.04) because low-cutoff filters strip noise energy → restored **sub-160Hz low THUMP bodies** for cushion+pocket (low+short sine = percussive thud, NOT a beep — the beep is only mid-range 300-800Hz sines) + cranked vols + master 1.15/lighter comp → peaks cue .38 / clack .55 / cushion .43 / pot .39, none clipping. ⚠ **AUDIO RULE learned:** mid sines = beeps (avoid on impacts); low short sines (<160Hz, <40ms) = needed thump bodies; a noise click through a low-cutoff filter is near-silent (needs a low body + high vol). All temp hooks grep-cleaned. ⚠ Registered by HAND (prepend + `build-sitemap.cjs`), NOT the banned `sync-registry-paths.cjs`. Hub `main.js?v=74`/`styles.css?v=84`.
- **SHIPPED `main 8e3e473` (2026-07-14): 3 new visual utility tools** (Tip Splitter 074, Pomodoro 075, World Clock Overlap 076) in the light/dark Geist `tools/` family. Owner directive: **tools must be visual-first, not text-heavy** — so each leads with an interactive control, not a form. **Tip Splitter** (`tools/tip-splitter/`): a **drag-dial** sets tip% (soft detents at 15/18/20/25 via SNAPS, SVG ring + handle positioned by cx/cy not transform — the transform-origin fight left a stray mark), people are **tappable person-icon glyphs**, per-person is the hero; round-up-to-even. **Pomodoro** (`tools/pomodoro/`): big **draining SVG ring**, auto-advancing focus/break phases (body[data-phase] swaps accent coral→teal), 4-dot cycle indicator toward the long break, Web-Audio **bell chime** on each switch (needs owner ear), `document.title` countdown, 15/25/50 length chips, sound toggle in topbar. **World Clock Overlap** (`tools/world-clock/`): **stacked day/night bars** per city (each bar = a 24-stop CSS gradient shifted by the city's `Intl` UTC offset), a green **"everyone awake" band** (8am-10pm local intersection), a **draggable now-line** (overlay must be `pointer-events:auto` to catch the drag), add/remove up to 5 cities from a curated ~41-city `<select>`, share via URL hash + localStorage. Default cities local+NY+London (they overlap; LA+NY+London+Tokyo has NO overlap so was a poor default). Full pipeline done + verified (0 console errors desktop+375px, light+dark, gallery search finds each, real rendered card+OG for all 3). ⚠ **GOTCHA I hit + fixed same session (`8e3e473`):** do NOT run `node scripts/sync-registry-paths.cjs` — it's a **one-off migration** that alphabetically SORTS the registry, rewrites every `path` to `/index.html` form, AND injects ~8 stale hardcoded entries. `reference.md` lists it as a routine command but it is NOT; it corrupted the registry (76→84, sorted, wrong paths) and I had to rebuild from `git show HEAD~1:tools-registry.json` + re-prepend the 3 tools with dir-form paths, then re-run `build-sitemap.cjs` (that one is safe — read-only on the registry). Registry order doesn't actually drive display (home=random 9, all-toys=alphabetical) but newest-first + `tools/<slug>/` dir paths are the convention (dir paths → clean `/tools/<slug>/` launch URLs via `toyHref`). Hub assets bumped `styles.css?v=83`/`main.js?v=73`.
- **SHIPPED `main e88fb9e` (2026-07-14): Nova Coil overhaul + Mini Golf audio rebuild + site-wide US-English/em-dash copy sweep** (115 files; live-verified). **Nova Coil** (`script.js?v=6`): (1) **varied coil silhouette per wave** — `buildTrack` now picks squircle / lobed-flower / egg / organic-wobble / round via a radial profile `pf(angle)` using only INTEGER harmonics of the screen angle, so every turn gets identical modulation and arms stay parallel (path always spirals to core); wave 1 stays round; `outerR` divides by profile max, `turns` clamps by profile min so arms keep ≥2.3-ball spacing; verified bounds+gaps+determinism 14 waves × 2 viewports. (2) **asteroids that block shots** — seeded into open pockets, radius = (nearest-track-dist − ball − pad) so chain never touches them, kept clear of cannon pocket + HUD/hint bands; projectile shatters on hit (`sndBlock`); aim tracer ray-clips at the first rock; `wantObs` ramps 0→4 by wave. (3) **Zuma pull-back** — chain split into segments at gaps; the TAIL segment is fed forward rigidly, every segment AHEAD of a gap retracts toward the survivors (`RETRACT=300`); rejoin with matching colors clears. (4) **exponential chain reactions** — cascade combo doubles the multiplier (`n*10*2^(combo-1)`, cap 8): verified 30→60→120→240→480→960; escalating shake + color `comboFlash` + rising `sndChain` + "CHAIN xN +pts" floaters + "CHAIN REACTION xN" banner; `comboT` window 1.15s; **fixed crash** when a cascade empties the chain mid-frame (`chain[0].s` guard). (Earlier same session, already in this commit: badge center→corner entrance slide.) **Mini Golf** (`script.js?v=11`): audio rebuilt from sine/triangle beeps into physically-modeled voices (putter tock, timber rail knock, hollow flagstick 1:2.76:5.40, cup drop-knock-rattle, pond plunk, speed-keyed felt-roll bed) through a compressor + smoothed-convolver reverb bus with per-voice sends + stereo pan; graph + peak-signal verified, **but headless can't hear it — NEEDS OWNER EARS** (both this and Nova Coil's chain sfx). **Copy sweep** (owner: US English + em dashes only if necessary): 3 subagents cleaned registry (64) + og-gen (47) + 66 HTML + 27 scripts → 0 prose em dashes / 0 British spellings in user-facing copy; **preserved** page `<title>`/og:title brand separators and `share.js`'s `split("—")` name-extraction delimiter (do NOT "fix" those); empty-state placeholder glyphs standardized to en dash `–`; cache-bumped 25 toy/tool scripts + `share.js?v=5` (unified across all 16 pages) + `suggest.js?v=3`. Global memory updated: `~/.ai/memory/DECISIONS.md` Content-voice + `human-voice` skill now carry "em dashes sparingly", US-English enforcement, and "UI copy is in scope". Verified: all JS parses, 13 sampled pages 0 errors, live nova-coil boots+plays 0 errors, share pill still extracts name. **⚠ Reusable badge-relocation pattern:** when a toy docks the tip-jar/fullscreen badges away from their right-center default, pair the CSS override with the center→corner entrance slide (see nova-coil `script.js` boot IIFE + `styles.css` `.opt-*.opt-*` block).

- **SHIPPED `main a80df4b` (2026-07-14): Mini Golf hole-variety fix** (owner played all 18 and "saw fairly similar holes throughout, mainly the water/bridge one"). Root cause: the daily `buildPlan()` drew each slot uniformly-random from a difficulty-tiered pool with ONLY adjacent-repeat dedup, and the `hard` tier's pool was 3/4 water (rapids/culvert/bridge) — so "hard" ≈ "water" and popular templates recurred hard. Old 20260714 course had **7 water holes** (bridge×3, oxbow×2, rapids×2). Fix (in `toys/mini-golf/script.js` — POOL + new WATER map + buildPlan rewrite): (1) **broadened the tier pools** so the dry terrain/obstacle holes span easy→hard (`easy` +ridge; `med` +dunes,windmill; `hard` +terraces,puttmore,ridge,pinball,dunes) — difficulty no longer = water; (2) replaced uniform pick with **least-used-first scoring** (`score = used*10; +300 if used≥2; +1000 adjacent-same; +100 back-to-back water; +500 over WATER_CAP=5`) after a deterministic per-day Fisher-Yates shuffle for tie-breaks. Verified across **40 consecutive days**: avg **12.7 distinct hole types/round** (27/40 use all 13), water **~4.5 and never back-to-back** (was up to 7 clustered), **no template >2×**, all 40 courses unique + deterministic (same day = same course across resizes), all 18 holes × 10 days build with 0 errors, toy boots+putts clean. Side effect (desired): back nine now mixes bumper/dune/hill holes, not just water/windmill; stiffer cup physics keep it hard. `script.js?v=9`. Card/OG unchanged (don't depend on the daily plan). Live-verified (200, v=9, WATER_CAP + broadened pool in deployed script). Reusable card-capture `window.__mg` hook (shot/plan/buildAll) was re-added for verification then grep-cleaned.
- Prior round (`39c53ce`, 2026-07-14): **Nova Coil two-bug fix + Mini Golf card/OG refresh.** **Nova Coil bug 1 (owner: "balls hitch onto the empty coil way ahead of the main chain, everything moves all the way up"):** a shot's insertion `s` came from `nearestS()` searching the WHOLE spiral, so on a loop-back the nearest track point could be a different arm far ahead (nearer the core) than the ball actually struck → inserted ball becomes leader → `insertBall` yanks the whole chain forward. Fixed with `nearestSNear(x,y, chain[hit].s, D*2.5)` — localise the nearest-point search to a ±2.5·D arc-length window around the struck ball (adjacent arms are ≥1 full turn = hundreds of px apart in arc length, so the window never spans two arms). Proven: on a phone-tight coil across 14 waves the OLD global search mis-snapped in ~50% of cases (worst 893px ≈ 34 ball-diameters); NEW caps every case at ≤9px. **Nova Coil bug 2 (owner: "donation thing on the side blocks some shots"):** cannon fires from dead-centre + click-anywhere-to-fire, but the shared tip-jar AND fullscreen badges dock at right-CENTRE (`top:50%`) — squarely in the rightward firing lane. Fixed with a page-scoped override in `nova-coil/styles.css` docking both to the bottom-right corner (`.opt-tipjar.opt-tipjar`/`.opt-fs.opt-fs` doubled-class out-specifies the components' injected styles; `transform:none`; `.opt-tipjar.opt-tipjar.tj-nudge{animation:none}` silences the right-centre-anchored nudge so it doesn't leap out of the corner). Versions `script.js?v=3`/`styles.css?v=4`. **Mini Golf:** regenerated `assets/cards/mini-golf.png` (captured the **Bumper Grove** pinball hole — day 20260715 hole 3 — square 1080², chrome hidden, ball nudged mid-arena; bright sculpted arena w/ beveled rail + drop shadow + 5 bumpers + ball + flag; replaces the old flat Jul-1 card) and `assets/og/mini-golf.png` (1200×630 via `scripts/og-gen.html#mini-golf`, which embeds the card via `img()`); refreshed the OG-gen desc + the toy's og/twitter meta to the daily-course line "A brand-new 18-hole course every day. Read the sloped greens, dodge windmills and water, and sink the putt." **Card-capture technique (reusable):** temp `window.__mg` hook (`shot(day,hole)`/`nudge(fx)`/`plan(day)`) drove the daily generator to a chosen photogenic hole, then grep-cleaned. Card images share a filename (no `?v=`) — same as every other rendered card — so a hard-refresh is needed to see a swapped card locally; live is fine after deploy. Live-verified: nova-coil 200 (v=3/v=4, nearestSNear + badge override in deployed files, chain no longer yanks, badges bottom-right, 0 errors); mini-golf card+OG 200 image/png with new byte sizes (card 1065593 B, OG 600308 B), gallery card renders the new 1080² image, toy boots Hole 1/18 + putts, og:description updated live. **Mini Golf still owner-only:** real play-test (difficulty/feel) + fanfare/pin/rim audio by ear (owner will "listen later"); card hero is Bumper Grove — swap to night-windmill or gold-finale on request.
- Prior round (`510eb6a`, 2026-07-14): **Magnetic Face (Wooly Willy) usability fix** — owner said it was "pretty hard to use." Root cause: the iron filings' max speed (~2.4 px/frame from `PULL 1.5` × `FRICTION 0.62`) was far below a normal drag (15–30 px/frame), so they crept and got left behind — pure center-attraction never carried the clump. Added a proximity-weighted **`CARRY` force** (`0.92 · t²`, `t`=1 at wand → 0 at field edge) so near filings inherit the wand's per-frame motion and a drag sweeps the whole clump like the real toy; widened field `R_FRAC 0.115→0.135` and pull `1.5→2.1` for easier grab/gather; sync `pmx/pmy` on press-down so grabbing doesn't jolt. `CARRY`/`PULL`/`R_FRAC` at the top of `script.js` are the feel knobs (lower `CARRY`→~0.7 if too grabby). `script.js?v=1→v=2`. Live-verified (200, v=2 tag, CARRY in deployed script, filings follow the wand + leave a comb trail, 0 console errors). **Open:** owner feel test — may want `CARRY` dialed.
- Prior round (`73dfb17`, 2026-07-14): **Mini Golf full course overhaul** (was in flight below; now LIVE + verified — daily "Tuesday, Jul 14" course renders, putt works, card/OG 200, 0 errors). **Open:** owner real play-test for feel/pacing/difficulty (much stiffer) + fanfare/pin/rim audio by ear; regenerate `assets/cards/mini-golf.png` + `assets/og/mini-golf.png` to match the new sculpted look (still show the old flat version). `SINK_SPEED 300` is the drop/bounce tuning knob. Full build detail ↓ — rebuilt `toys/mini-golf/` into a sculpted 18-hole course (~1580-line script rewrite). **Engine:** greens = SDF plate unions (rounded-rect/circle/capsule; wall = push along SDF gradient), terrain = gaussian hills/bowls + tilt the ball physically rolls on (per-pixel NW-sun shading + downhill chevrons baked into the cached scene), fixed 1/240 substeps, static friction that scales with slope (`STOP + slope*0.6` — pure STOP threshold left balls micro-creeping forever on gentle tilts), settle-valves at 9s/14s. **Hazards:** raked sand (6.8/s friction, grooves stamped into a persistent overlay), water (+1 penalty, last-dry respawn; wall reflection is SKIPPED where green borders water so the ball rolls off the lip), windmills (capsule blades, relative-velocity impulse; blades also wake settled balls), mushroom kickers (min-exit 340 but ONLY on impacts >120 px/s — else bowl+kicker = perpetual-motion softlock), boulders, culvert tunnel (transit owns the ball — settle-during-transit once killed the exit velocity). **Course:** 12 hand-authored templates + gold par-5 finale; back nine = mirrored + renamed reprises (Undertow/Switchback/Night Mill/Moonrise Summit/Firefly Grove); 6 day-cycle palettes (dawn→noon→aft→dusk→night→gold); course par 57. **⚠ HARD-WON LESSONS:** (1) plate joins MUST overlap deeply — radii scale with the CROSS axis but positions with the LONG axis, so tangent joins seal at other aspect ratios (pinball neck sealed on phones; landscape-phone killed 5 holes) — bury connector capsules to arena CENTERS; (2) `rrUV` had to normalize corners AFTER the mirror transform (pre-sorted corners invert into negative-height rects when mirrored); (3) a mill gate needs a clear lane running the WHOLE segment — an upstream corridor narrower than the swept disc funnels every shot into the blades (finale was a 3% lottery); (4) verify with a tee→cup **flood-fill over the ball-eroded SDF at 4 viewports** (430/1240/568×320/375) — random-putt probes miss sealed throats. Ultracode run: design jury (3 lenses) → build → verify fleet (visual/code/gameplay agents) — fleet found 2 criticals + resize-teleport + settled-ball-vs-blades + culvert dead code, all fixed + re-verified (all-viewport connectivity PASS, full-round bot completes, 0 console errors, widgets on scorecard, debug hooks grep-clean). **Daily course + harder (2nd pass):** the whole 18 is now **randomly generated per local date** — `buildPlan()` seeds a mulberry PRNG off `todayNum()` (YYYYMMDD) and draws each slot from difficulty-tiered pools (`TIERS`/`POOL`: open→easy→med→hard→fin, harder weighted to the back nine), day-cycle palette by slot, repeats auto-mirrored + renamed (`REPRISE`); everyone gets the same course today, a new one tomorrow; `Math.random()` stays only for cosmetic motes/confetti so layout is fully deterministic (verified). Per-day best score keyed `golf_day_<num>`; scorecard says "<Weekday>'s course … a brand-new course drops tomorrow"; hole-1 toast is a daily banner. **Difficulty** (owner won too easily): the cup was a vacuum — `CUP_GRAB` 2.1→1.5×CUPR, `CUP_PULL` 520→300, `CUPR` 1.55→1.42×BR, `SINK_SPEED` 320→260 (more lip-outs), `FRICTION` 1.7→1.55, `REST`→0.76, regular windmill `omega` 1.45→1.6. **Verified after both passes:** tee→cup flood-fill connectivity PASS across **8 days × 4 viewports × 18 = 576 hole-instances**; daily determinism + variety (5/5 distinct plans, opener/finale anchored, no adjacent repeats); skill-aware bot finishes fairway in 3–5 and windmills in 5–7 (par 3); 0 console errors; debug hooks (incl. `__mg.setDay`) grep-clean removed. **Designed intro cards (3rd pass):** the hole/daily intro is no longer a long text toast — it's a proper DOM placard (`#holeCard`, `.hc-*` styles): gold pip-flanked eyebrow ("HOLE 7 OF 18" / "DAILY COURSE" / "⛳ THE FINAL HOLE"), cream-gradient display title (hole name or the date), par/meta line; the daily variant adds a hairline-ruled "18 HOLES · PAR N" + "Hole 1 · <name>" footer. Fades/slides in, `pointer-events:none`, clears the HUD on landscape, all long lines `white-space:nowrap`. `showToast` stays for transient gameplay messages. **Cup physics (4th pass):** replaced the soft "vacuum" cup with a funnel + physical rim — a **funnel gravity** (steeper at the lip) curls good lags in; over the mouth the **entry speed decides** (`< SINK_SPEED 300` drops, faster commits to `cupCross` with a one-time ×0.68 rim energy-loss and pops out the far side — a slow overspeed ball then gets pulled back and rattles in); a glancing ball whose **impact parameter** clears the centre by `>0.55·CUPR` **horseshoes** around the rim (cancel inward radial, keep tangential, shave speed) and rolls on past. Bug found in testing: the first rim model cancelled inward radial for ANY annulus ball, walling off dead-centre putts (never reached the mouth) — the impact-parameter gate fixed it. Ball does a scale-pop (`cupHopT`) + a `sndRim` rattle on lip-out. **Pin/flagstick pass (5th):** reworked again around a central **pin** — the funnel curls lags in; a **slow ball drops** (fires at `d<CUPR` before the pin zone); a **fast ball bounces off the flagstick** (`PIN_R=0.4·BR`, reflect about contact normal ×`PIN_REST 0.55` → dead-on comes straight back, off-centre ricochets at an angle, `sndPin` knock); a fast **outer-rim graze slides around** and rolls on. The **pole+flag now draw live** (moved out of the baked scene) and **wiggle on a hit** — a damped sway (`poleSway()`: `sin·e^-decay`, ~12px peak, ~4 reversals over 1.5s) knocked in the ball's travel direction; pin base ferrule baked into the cup. Verified: 64/64 softlock shots resolve, slow putts still drop at every offset, sway oscillates+decays, 0 errors, hooks grep-clean. `SINK_SPEED 300` is the drop/bounce threshold (owner tuning knob). Versions: `script.js?v=8`/`styles.css?v=5`, hub `main.js?v=72` (registry copy + tags updated; note `all-toys` is a DIRECTORY now — bump `all-toys/index.html`, not all-toys.html). **Needs: owner play-test (feel/pacing/difficulty now much stiffer), fanfare by ear, then card+OG regen to match the new look.**
- Prior round (`9db1889`, 2026-07-14): **Perfect Circle now hosts the More Games widget on canvas** — PC has no modal (score renders on the canvas), so the widget + a new Share pill live in a bottom-anchored DOM strip (`#pcCta`) over the canvas, revealed the moment the first score lands and left pinned (no per-stroke flicker). Score-aware share text. **`share.js` generalized** to mount into an explicit `[data-opt-share]` host (not only `#overlay .panel`) — the reusable pattern for canvas toys (mini-golf could adopt it). Perfect Circle moved **promote-only → widget host** (still self-excluded). Versions: PC `script.js?v=2`/`styles.css?v=3`, `share.js?v=4`, `more-games.js?v=4`. Live-verified (strip reveals, 2 cards, no self-link, pill readable, score in share text, 0 errors).
- Prior round (`0323a6f`, 2026-07-14): **"More Games" cross-promo widget** — new shared `assets/more-games.js` auto-mounts a "More Games" panel into a toy's `#overlay .panel` (start + end screen), showing 2 random portfolio games excluding the host, with the same design-matched SVG icons as the five-second-game `MoreGames.tsx` component (that repo is the origin list — keep the two in sync; it now also carries Perfect Circle + these entries). Host slug is parsed from the canonical URL; wired into **14 toys** (alpenglow, dot-loop, deep-descent, nova-coil, puffling, paper-plane, mini-golf, random-maze, sky-fortress, slice-it, stack-tower, trio, solitaire, blackjack). **Mini Golf + Perfect Circle have no overlay panel → promote-only (widget can't mount, silently no-ops).** Solitaire + Blackjack deal instantly (overlay = end-only), so their widget shows only on game-over; the other 12 show it on the start modal too (owner OK'd start-modal exposure). **Contrast lesson (resolves the Sky Fortress share-pill gotcha below): most toys leave `.panel` `color` unset → default black, so anything using `color:inherit` renders black-on-dark.** Fixed both surfaces with a **luminance-aware self-contained theme** (walk up to first opaque bg → light/dark scheme, explicit colors, never inherit): `more-games.js` names 13–18:1, tags 4.9–7.7:1, label 6.4–6.9:1; **`share.js` pill is now luminance-aware too** (was `color:inherit` at .6 → invisible on ~11 toys; now 5.5–8.4:1, bumped to `?v=3`, applies site-wide incl. coin-pusher). Card names wrap to 2 lines (no truncation); puffling's light panel correctly flips to the dark-text scheme. Full overlay contrast sweep of all 16 overlay toys passed (gradient `background-clip:text` titles + gradient buttons read as false-positive 1:1 to a luminance sampler — verified real via computed styles). Versions: `more-games.js?v=4`, `share.js?v=3`. Live-verified on nova-coil (widget mounts, no self-link, pill color `rgba(255,255,255,.9)`, 0 errors). ⚠ **`five-second-game` repo (Lovable-connected) has the matching `MoreGames.tsx` Perfect Circle entry uncommitted — NOT pushed by this session** (owner was editing it; pushing there syncs to Lovable + needs a Publish).
- Prior round (`826e219`, 2026-07-09): **NEW 073 Alpenglow** (`toys/alpenglow/`, category `game`) — the backlog Alto's-style trick sandboarder, Canvas 2D, one-touch (press = jump, hold = backflip at 430°/s, release = stop; auto-level rescues released boards <0.45s from landing). **Flywheel:** bank = sum × chain on clean landing → decaying boost (speed + smash-through) → **HOT ×2** if you bank again while still boosted; boost raises the speed ceiling 25%. Terrain = cosine keypoints (mean 12° downhill, uphill clamped to kickers), chasms + bunting rails; **physics and render share ONE `groundY()` surface** (snow, chasm wall ramps, pit floor) — this took THREE iterations of play-test bugs (pop-up-to-surface, through-the-wall, through-the-floor) before unifying; don't reintroduce separate collision geometry. Rail wire clearance = dense terrain sampling (the lift formula's sign was inverted once, and keypoint-only checks miss cosine bulges — sim-verify 0/20k). **Key lessons:** the blueprint put dive (2.2×G) on the same hold as backflip which made flips impossible — dive was CUT; world-anchored popups are unreadable at speed — screen-anchor them; the pad's breath LFO rides ON TOP of its muted base gain — gate it separately for true end-screen silence; chunk render-caches must clear on world reset. Art: 5 HSL-lerped palettes over an 8-min day cycle, weather axis (snow/blizzard/storm/fog/rare rainbow), rare slow aurora on clear nights, god rays, depth haze, snow glints, bird flocks, verlet scarf pinned to an animated shoulder; rider = jointed pose-blend silhouette (crouch/stretch/tuck/scrape/grind/ragdoll + torn-away board); 0.82× slow-mo on big air; smashable trees (splinter fast/boosted +15, rustle slow, never lethal). Audio: pad + generative piano (Vapor port) + carve bed (returns on landing) + grind hum + wind/rain/thunder, all gated silent on the end screen. Both this and Sky Fortress got a **compact-overlay breakpoint for landscape phones** (max-height 520px, verified to 568×320). Versions: toy `script.js?v=15`/`styles.css?v=3`, Sky Fortress `styles.css?v=3`, hub `styles.css?v=82`/`main.js?v=71`. Live-verified (toy 200/0 errors, home features it, card/OG 200, registry newest of 73). **Audio by ear + phone touch-feel still open** — 15 desktop play-test rounds done, all mechanics owner-tuned.
- Prior round (`6a0af84`, 2026-07-09): **072 Sky Fortress** (`toys/sky-fortress/`, category `game`) — the backlog "Zaxxon" item as a **real-3D raw-WebGL** isometric scroll-shooter (no libs, no build): low-FOV camera down the diagonal (45° yaw ~32° elevation; portrait fits by aspect-keyed camera distance), streamed ring-buffer fortress, **altitude is the mechanic** — hard cast shadow on the deck (ground-shader spot keyed to height) + side altimeter; door/low/band walls (amber top line = fly over, cyan jambs = thread the gap), crackling force-field beams between pylons at 3 heights, turrets with predictive flak orbs, weaving drone squads, green fuel silos (+32 fuel, guaranteed ≤260m apart; constant fuel drain = the soft timer). Auto-fire lasers; one hit = down; score = 2×distance + kills, best in localStorage, confetti+fanfare on new best. **Engineering notes:** all render positions ship-relative in z (dodges float32 drift on long runs); deck shader patterns wrap mod-96 with a periodic hash (else the whole floor repaints every 96u); additive sprite batch is premultiplied (`blendFunc(ONE,ONE)`); sphere-vs-AABB collisions; **ship roll/pitch clamped by ground clearance** (`asin(clearance/wingspan)`) so wings never clip the deck. **Input gotcha:** at this camera angle screen-right = world −x, so the drag/arrow lateral mapping is deliberately FLIPPED in code — don't "fix" it. **Share-pill gotcha:** share.js's pill is `color:inherit` — the overlay `.panel` MUST set a `color` or the pill renders invisible on a dark panel. Audio: detuned-saw engine (pitch ~ speed+altitude), pitch-varied panned lasers, noise explosions, climb whoosh, low-fuel beeps → compressor + convolver bus, iOS unlock + Sound toggle. Versions: toy `script.js?v=2`/`styles.css?v=2`, hub `styles.css?v=81`/`main.js?v=70`. Desktop play-test round done (copy, wing-clip, share-pill fixes in same commit). Live-verified: toy/card/OG/registry all 200, 0 console errors, home features it. **Owner marked it DONE 2026-07-09** — no open items.
- Prior rounds: **071 Rain Stick** (`35dd2de`, tilt-and-pour granular rain; sleep-system physics; real-device audio + gyro test STILL OPEN — `gyroSign` may need flipping, drag works everywhere) · **070 Marble Machine** (`b97eedd`, pseudo-3D programmable marble music machine + virtual-URL hub refactor; audio play-test still open). Full detail: [archive/HANDOFF-archive-2026-07-08.md](archive/HANDOFF-archive-2026-07-08.md) + git log.
- Playwright gotchas: force-clicking a `visibility:hidden` button silently hits the canvas underneath — use DOM `el.click()` via `evaluate` when chrome is hidden. Headless sim time vs wall time diverge (pumped rAF runs fast; setTimeout stays real) — probe internal state via a temp debug hook instead of comparing screenshots taken at different wall-times.

**⚠ Cross-promo deploy model (owner, 2026-07-14):** the sibling repos in the "More Games" network — **`five-second-game` (5secondgame.com) and `the-trail-game` (thetrailgame.com) are on Vercel/GitHub, NOT Lovable** — a `git push origin main` auto-deploys, no "Publish in Lovable" step (the `lovable-tagger` devDep is a leftover scaffold artifact). When adding a toy to the cross-promo, edit `assets/more-games.js` HERE + `MoreGames.tsx` in BOTH sibling repos (`five-second-game/src/components/game/`, `the-trail-game/src/components/site/`), keeping the entry identical, and bump `more-games.js?v=N` across the ~16 toy pages that load it. **Done 2026-07-14: Pool + Mini Golf are in all 3 lists** (Mini Golf pre-existed; Pool added everywhere, felt+8-ball favicon, `more-games.js?v=5`, all pushed/auto-deployed).

**Next step:** **Owner audio-by-ear pass on Shuriken Night** (the new hit / slash / temple-gong death especially — headless can measure level and spectrum but never taste) plus a real-device touch check (tap to throw, drag to turn, hold to charge). Then **ticket earn-rate tuning** via `?tickets=1` on real sessions before building **Phase 2** (store page + trophy shelf + gag items + share loop). Backlog also holds Skee Ball (the natural showcase for tickets), Downland, Clowns & Balloons. Then owner tests it via `?tickets=1` on any toy and we tune the earn rates before building Phase 2 (store). Also still open: owner **audio-by-ear pass on Spelling Blocks** (wooden-block clacks / place / win — headless can't audition) + a real-device feel check, and decide on the **sibling-repo cross-promo** for Spelling Blocks (add to `five-second-game` + `the-trail-game` `MoreGames.tsx` — each push auto-deploys a production site, so gated on OK). Also still open: owner audio-by-ear on the 7 generative visual toys (Mandelbrot drone + 6 soundbeds); Alpenglow/Rain Stick/Marble Machine real-device audio; Slice It fruit-render polish (logged). See `.ai/memory/BACKLOG.md`.

## ⚠ Live conventions (accreted from builds — bake into every new toy)

- **Directory URLs:** canonical/og:url/JSON-LD/registry path/sitemap all use `onepagetoys.com/toys/<slug>/`; back-links are `../../`.
- **Safe-area insets:** fixed-position UI (tip jar, fullscreen, sound toggles) must use `max(16px, env(safe-area-inset-*))` — `viewport-fit=cover` is live on some toys and the notch will cover corners otherwise.
- **iOS tap highlight:** full-bleed canvas toys MUST set `-webkit-tap-highlight-color: transparent` on html/body/canvas (+ `-webkit-touch-callout:none`/`user-select:none`) or iOS washes the screen blue per tap.
- **Cache-busting:** bump `?v=N` on EVERY edit the owner will test — a stale `script.js?v=1` once cost a whole play-test round.
- **Quote canvas font families containing digits** (`'Baloo 2'`) — Safari treats the unquoted string as invalid and silently falls back to `10px sans-serif`.
- **Rotation-invariant world scale:** viewport-relative world scaling changes difficulty when aspect flips — key world scale to the device's long dimension.
- **Media-query order:** equal-specificity base rules later in a stylesheet silently override earlier `@media` blocks — check COMPUTED styles, not rule presence.
- **Real WebKit for Safari-only bugs:** `npx playwright install webkit` — headless Chromium can't reproduce Safari rendering (font-string fallback, tap highlight, shadowBlur rasterization).
- Older lessons (3D box-shadow → outline, iOS audio unlock, headless rAF throttling, etc.) live below, in the archive, and in `.ai/memory/DECISIONS.md`.

## ⚠ Design quality bar — READ BEFORE BUILDING ANY TOY
The owner is a **designer** and holds a high visual bar. A toy is NOT done when it merely "works" — it must look *intentional, polished, and richly itself*. The first desktop pass at toys 020–023 was rejected for being flat/crude with unrepresentative cards; this is the standard that pass missed:
- **Every toy MUST be interactive** (owner rule, 2026-06-22). No purely passive "just watch" toys — each must respond to tap / drag / click / controls in a meaningful way (e.g. Lava Lamp: drag to stir the wax; Spirograph: gear sliders; Plasma Ball: arcs follow your finger). If a concept is inherently ambient, add a way to disturb, shape, or steer it.
- **Each toy is its own curated little world** — a deliberate palette, lighting, depth, motion. No flat primitives on flat backgrounds. Study the strongest live toys before building and match that level: **Goo Cursor, Star Click Sky, Tiny Idle Garden, Falling Sand, Three Doors, Plasma Ball**.
- **Render real-world toys *as the real object*** — material, lighting, shadow, glow. (Newton's cradle needs a grounded frame + chrome balls, not flat dots. A spirograph needs dense layered glowing rosettes, not one thin loop. Plasma needs jagged crackling lightning, not smooth rotating lines.) [Note: Lava Lamp is intentionally full-viewport metaballs now — NOT a vessel — per owner request; don't re-add a lamp shell.]
- **Card images MUST clearly represent the toy.** Strongly prefer a **real rendered thumbnail** captured from the finished toy (`assets/cards/<slug>.png`, overlays hidden, square-ish crop) over abstract CSS gradients — abstract blobs that don't read as the toy are NOT acceptable. The card CSS is then `background:#<bg> url("cards/<slug>.png") center/cover`.
- **Never ship without an OG image.** Generate one for every toy via `scripts/og-gen.html` (you can reuse the rendered card thumbnail as the motif — see the `img()` helper + toy entries already in that file). A missing `assets/og/<slug>.png` = a broken share link.
- **Always SCREENSHOT and look at it** (toy, card, and OG) before committing — "no console errors" is not the bar; *how it looks* is. Note: headless Chromium throttles `requestAnimationFrame`, so animated toys may not finish drawing in a normal screenshot — drive frames with `await page.evaluate(()=>new Promise(r=>requestAnimationFrame(r)))` in a loop, or the toy will look unfinished when it's actually fine.

## ⚠ Audio quality bar — READ BEFORE BUILDING ANY TOY WITH SOUND
Sound is a **first-class part of the presentation**, held to the same standard as the visuals (owner directive, 2026-06-30). A toy's audio isn't done when it plays the right note — it must sound **rich, spacious, and intentional**. Full standard + reference recipes in `.ai/memory/DECISIONS.md` ("Audio quality bar"). The essentials:
- **Layered, physically-grounded voice:** `transient + body (correct harmonic/inharmonic partials) + decaying tail`. Know the physics — clamped-free bar (kalimba) is inharmonic ≈1:6.27:17.55; a tuned tongue drum/hang is harmonic (strong octave ×2, twelfth ×3); a tube (chimes) ≈1:2.76:5.40. Right partial ratios = "real instrument" vs "beep."
- **Space is non-negotiable:** convolver reverb (smooth impulse — low-pass the noise so the tail isn't grainy; highshelf for shimmer, highpass so lows stay clean) + a feedback **delay** where it fits (feed it into the reverb so repeats bloom). Dry = cheap.
- **Stereo width** (pan voices by on-screen position), **bus compressor** (glue + anti-clip on strums), master lowpass for silk, **velocity/detune variation** (no machine-gun sameness), **consonant scales** (pentatonic/Akebono), **iOS unlock** + Sound toggle.
- **Headless CANNOT hear** — the automated pass only proves the graph builds. Audio is always "needs the owner's ears"; say so and iterate on feedback. Copy **Kalimba** / **Steel Tongue Drum** audio architecture as the starting point.
- **Meta-principle:** *keep pushing the presentation envelope — visual AND audio — as far as we dare within reason.* Each release a notch more polished than the last; when touching a toy, look for the upgrade, not just "works" (without breaking the zero-build vanilla soul).

## To continue on another machine
1. `git pull` (the remote is authoritative; everything below is committed/pushed).
2. Run locally — **no build, no deps needed**: from the repo root, `python3 -m http.server 8000`, then open `http://localhost:8000`.
3. Only if you need the headless verify / screenshot / OG-render workflow (Playwright): `npm install` then `npx playwright install chromium` (add `webkit` for Safari-only bugs). **`node_modules` is gitignored on purpose** (it once bloated pushes), so it won't come down with the pull — reinstall it.

## What this site is now (post-pivot, 2026-06)
A branded **launcher hub** + **69 standalone, full-bleed toys**, each in its own `toys/<slug>/` (or `tools/<slug>/` for the lone utility), opening in a NEW TAB. Geist design system (Geist Sans/Mono, neutral grays, restrained red `#941e1e`, 3-way System/Light/Dark theme). The old ~57 toys were archived to `/archive/`. **Direction: FUN / playful / experiential — NOT dev tools** (dev tools belong on the separate BuildUtilities site). Static site, GitHub Pages from `main` → onepagetoys.com. The authoritative toy list is `tools-registry.json` (newest first) — don't maintain a prose copy here.

## Key files
- `tools-registry.json` — drives the gallery. Prepend new toys (newest first). slug/name/shortDescription/category/tags/status/path. ~7 concept tags per toy (mechanic/vibe/input/theme/comparables), always keeping each toy's NL-phrase key.
- `assets/main.js` — gallery render + search; `TYPE_NL_PHRASES` map (natural-language search terms per tag); home shows a RANDOM 9 (featured toy excluded); GA4 events (`toy_launch`, `outbound_click`, `tip_jar_click`, `share`). Cache-bust `?v=N`.
- `assets/styles.css` — hub styles + per-slug `.card__preview[data-slug="…"]` thumbnails (+ a `:not()` default-exclusion list). Cache-bust `?v=N`.
- `assets/theme.js` — 3-way theme toggle. `assets/tip-jar.js` — Mighty Army shield badge → PayPal. `assets/share.js` — drop-in share pill for overlay games (12 wired). `assets/fullscreen.js` — shared fullscreen button.
- `sitemap.xml`, `assets/og/<slug>.png` (per-toy share images — every toy has one), `assets/cards/<slug>.png` (real rendered card thumbnails — new toys use a rendered thumbnail per the quality bar above).
- **`scripts/og-gen.html`** — parameterized 1200×630 template for per-toy OG share images. Open `file://…/scripts/og-gen.html#<slug>` and screenshot at 1200×630.

## Conventions / how to add a toy
- New `toys/<slug>/` = self-contained `index.html` + `styles.css` + `script.js`, full-bleed, frame corners (`No. NNN`, name, back-link `../../`), a hint line, includes `assets/tip-jar.js` (current `?v=`) + `assets/share.js` for overlay games. Full SEO meta + JSON-LD + GA snippet + no-flash theme init (copy a recent toy as a template).
- Register: add to `tools-registry.json` (top) + `sitemap.xml` + `TYPE_NL_PHRASES` in `main.js` + a `.card__preview[data-slug]` rule in `assets/styles.css` (and add the slug to the default `:not()` chain) + a per-toy OG image via `scripts/og-gen.html`. Directory-form URLs everywhere (see Live conventions).
- **Audio toys (LEARNED):** always add the iOS silent-buffer unlock + `resume()` inside the first user gesture, or Web Audio can stay silent on iOS Safari. Tune tones to be consonant + calm.
- **3D-transformed elements (LEARNED):** on a `transform-style: preserve-3d` element, `box-shadow` rings render with a clipped edge — use `outline` for focus/selection rings instead.
- Verify each toy headless before committing (Playwright): no console errors, no horizontal overflow at 375px, interactions work. Screenshot to eyeball visuals. Remove temp debug hooks (grep-clean) before commit.
- The full build checklist lives in the **new-toy skill** (`~/.ai/skills/new-toy`); this file carries the repo-local specifics it points at.

## Open ideas / next candidates (fun-aligned)
See `.ai/memory/BACKLOG.md` (Backlog-Viewer format; sandboarder marked `next`). Genre gaps: more audio, more cozy/idle. Skip as too-simple: the reflex cluster + one-shot gag generators in `/archive/`.

## Other repos (separate)
- **BuildUtilities** (`~/Personal Projects/buildutilities` = GitHub `brentspore/buildutilities` = Lovable project "BuildUtilities.com") — the dev-tools site; git push syncs into Lovable, then Publish in Lovable to deploy. Different repo entirely.
