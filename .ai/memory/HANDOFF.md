---
name: Cross-device handoff
description: Resume-here state + how-to-continue, kept IN the repo so it travels via git pull between machines
type: project
---

# Handoff — resume here

This doc lives in the repo (`.ai/memory/`), so it syncs between devices via `git pull`.
It is the authoritative current-state bridge. (A richer running log lives in the *global*
`~/.ai/memory/PULSE.md`, but that is machine-local and may NOT exist on every device — trust
this file when they disagree, and note `project.md` here is from the pre-2026-06 era and is stale.)

**Last updated:** 2026-07-01 · **Latest pushed commit:** `main 069cb3c` (**54 toys live**; `.claude/settings.json` + `.claude/settings.local.json` left out as always). ⏳ **UNPUSHED, awaiting owner `push`: NEW 055 Blackjack.** **055 Blackjack** (`toys/blackjack/`, category `game`) — owner: "let's do blackjack like in a casino." Full casino Blackjack in vanilla Canvas 2D, reusing Solitaire's card-face rendering + audio bus. **6-deck shoe** (auto-reshuffles under 26 cards), **chip betting** (5/25/100/500 poker chips drawn on canvas — click to stack a bet in the bet circle; click the circle to clear), **bankroll persisted** in `localStorage` (`bj_bank`, default $1000, auto-rebet last wager), green-felt table with arc markings ("BLACKJACK PAYS 3 TO 2", "DEALER MUST STAND ON 17 · INSURANCE PAYS 2 TO 1"), card fly-in animation from a top-right shoe. Full rules: **hit / stand / double down / split** (up to 4 hands; split aces get one card; splittable on same rank or any two 10-values), **insurance** (offered on dealer Ace up, pays 2:1), dealer **stands on all 17** (S17), **blackjack pays 3:2**, per-hand value badges (BUST/BLACKJACK/WIN/PUSH/LOSE), active-hand marker, headline result message. HTML action bar (contextual Deal/Clear · Hit/Stand/Double/Split · Insurance) + top controls (bank chip, New shoe, Sound). Synth audio: card deal/flip, clay chip clack, win arpeggio, lose fall (bus→convolver reverb, iOS unlock, Sound toggle). Real rendered **card** (`assets/cards/blackjack.png` — dealer K+hole, player A-K "BLACKJACK", $100 bet) + **OG** (No.055, GAME · CARDS, accent `#f2c85a`). Registered everywhere (registry prepend `game`, sitemap, NL `blackjack`, card rule bg `#0b5238` + `:not()` chain, og-gen entry). Hub cache-bust **`styles.css?v=53`/`main.js?v=51`**. Temp test/card hooks (`window.__bj`) **removed before ship**. Verified headless (DPR2): registry parses (**55**, first = blackjack/game), 0 console errors (toy + gallery), no 375px overflow, all rules logic-tested (win +bet, blackjack pays 3:2, bust, split deducts 2nd bet, double doubles bet + one card, insurance offered on dealer Ace), gallery card bg resolves. ⏳ Owner to play-test (feel, bet/payout math, split/double/insurance, audio by ear) + `push`. — Prior ✅ **PUSHED `main 37821c5` (+ handoff `069cb3c`): (a) NEW 054 Solitaire + (b) Slice It bomb-visibility fix.** **054 Solitaire** (`toys/solitaire/`, category `game`) — owner: "how about solitaire? or other card games?" → chose **Klondike** from 4 offered (Klondike / Pyramid / Blackjack / Video Poker). Full classic Klondike in vanilla Canvas 2D: 7 tableau columns + 4 foundations + stock/waste, **real playing-card rendering** (pip layouts for 2–10, framed court cards, corner indices, red lattice backs), **drag valid descending alternating-color runs**, **double-click auto-to-foundation**, **undo** (snapshot stack), **Draw 1/3 toggle**, moves+timer HUD, **bouncing win cascade** (cards launch + bounce off the bottom onto a persistent layer), green-felt table, synth card audio (flip/deal/place/foundation-ding/bad/win → light convolver reverb, iOS unlock, Sound toggle). Rules verified headless: correct deal (24 stock / 28 tableau, tops face-up), draw+recycle, undo, drag tableau→tableau w/ auto-flip, double-click waste-Ace→foundation, win→cascade+overlay, 0 console errors, no 375px overflow. Real rendered **card** (`assets/cards/solitaire.png`, landscape mid-game board) + **OG** (No.054, GAME · CARDS, accent `#f2c85a`). Registered everywhere (registry prepend `game`, sitemap, NL `solitaire`, card rule bg `#0c5238` + `:not()` chain, og-gen entry). Hub cache-bust **`styles.css?v=52`/`main.js?v=50`**. Temp test/card hooks (`window.__t`) **removed before ship**. **⚠ Responsive fix (owner "the cards need to be responsive"):** the first pass sized cards only by WIDTH (fit 7 columns) so on short/wide windows the top row + long tableau ran off the bottom, and on mobile the wrapped control bar overlapped the top row. Fixed: card size is now **width- AND height-constrained** (`cardW = min(widthFit, heightCap/1.4, 132)`), the **fan compresses adaptively** so the longest current column always fits the vertical budget (`positionAll` recomputes `fanDown/fanUp` from the longest column), and **the board's `topPad` now clears the measured control-bar height** (`controlsEl.getBoundingClientRect().bottom + 14`) so controls never cover cards at any width. Verified fits with no clipping/overflow at 1280×620 (wide-short), 375×720 + 414×896 (mobile), 820×500 (very-short). **Waste fan (owner "I want to see the previous 2 cards in the draw / the card left behind from picking one up"):** the waste now always fans the **last up-to-3 cards** (`shown = min(3, waste.length)`, was draw-mode-dependent showing only 1 in Draw 1) so the two prior draws stay visible behind the playable top card, and the card left behind is visible after you play the top. **Card-face declutter (owner "the card face designs get busy"):** the tableau render was applying a drop-shadow to the WHOLE face so every pip cast a blurry shadow (muddy/busy), and pips were oversized. Fixed: the soft shadow is now **baked into the card body only** (set on the base fill, cleared before drawing index/pips so symbols stay crisp), pips slimmed (`w*0.26→0.19`), corner index compacted (`rank 0.26→0.22`, `suit 0.22→0.16`), court letter/suit + Ace pip reduced; dragged run gets its own lift-shadow via a base-fill pass. Reads like clean real cards now. **Card + OG regenerated** to match. **Draw 3 is now the DEFAULT** (owner) — `draw3` inits `true`, button starts "Draw 3"/aria-pressed true (toggle to Draw 1 still available). **Double-click auto-play (owner "I should be able to double-click a card to play it without dragging it" + "double-click a card to move everything on top of it"):** double-tap/dbl-click any face-up card → `autoPlay(hit)` runs the whole grabbed run (`hit.run` = clicked card + everything on top of it): a single card tries the four foundations first, then any legal tableau column (prefers a non-empty landing over an empty one, skips the source col); a multi-card run only tries tableau columns. Uses the shared `performMove(run,src,dest)` helper (pushUndo/flip/afterMove). Double-tap window widened to 420ms; single-card play covers both "send home" and "move onto another pile." **Snap-back animation (owner "if I drop a card on a spot it will not go, animate it back to where it started from"):** an invalid drop no longer instantly teleports — `dropRun` now sets a `snap` tween `{run, fromX/Y (drop point), toX/Y (home slot), t, dur:0.16}`, `render` interpolates the run back with easeOutCubic while `frame` advances/clears it, and `sndBad()` plays. `isDragged`/render skip the snapping run so it draws in-flight. Verified headless: invalid drop keeps all 52 cards (count 52→52), 0 errors. `solitaire/script.js?v=6`. **Slice It bomb-visibility fix** (owner "make sure the bombs don't blend in too much to the background") — bombs were dark gunmetal on the dark-purple arena; added a **pulsing red danger halo** + lifted the body tone (`#6b7280→#2a2f37→#0d0f13`) + stronger red warning rim → pops clearly now (`slice-it/script.js?v=3`). — Prior ✅ **PUSHED `main 5c77149` — Slice It fruit-speed tune** (owner "the fruit might be moving too fast" → GRAV 1500→1150 for ~14% more hang time, `slice-it/script.js?v=2`). — Prior ✅ **PUSHED — (a) NEW 053 Slice It + (b) the Countdown custom date+time picker.** **053 Slice It** (`toys/slice-it/`, category `game`) — owner: "another super fun toy, something viral and extremely fun" → chose **Slice It (Fruit Ninja-style)** from 4 offered (Slice It / Ferrofluid / Chain Reaction / Rope Swing). A juicy fruit-slicer: swipe a glowing blade through fruit tossed up in arcs → each slices into two tumbling **half-moons** (clipped half-disc body + flesh cross-section + seeds) with a juice-particle burst + a persistent splatter layer; **combos** (≥2 fruit in one swipe → gold "N× COMBO +N" popup + pentatonic chime + bonus), **bombs** (black sphere + fuse spark → slicing one = instant game over w/ screen-shake + flash + boom), **3 misses (dropped fruit) = game over**, score + `localStorage` best (`slice_best`) + new-best confetti. Blade = additive glowing trail following pointer (mouse slices on move; touch needs finger-down). Full synth audio per the quality bar (layered juicy slice, whoosh, combo chime, bomb boom, miss thud, game-over — bus→compressor→convolver reverb, iOS unlock, Sound toggle). Deep-purple arena bg + vignette. Real rendered **card** (`assets/cards/slice-it.png` — sliced watermelon + blade arc + fruit) + **OG** (No.053, GAME · ARCADE, accent `#ff7a8f`). Registered everywhere (registry prepend `game`, sitemap, NL `slice-it`, card rule bg `#08050f` + `:not()` chain, og-gen entry). Hub cache-bust **`styles.css?v=51`/`main.js?v=49`**. ⚠ **Fixed during build:** slicing required a prior pointerdown (mouse wouldn't cut until you clicked once) → now `isMouse` from the move event's `pointerType` so mouse-move always slices; and gave halves a stronger perpendicular split + initial offset so they part into clear half-moons instead of a pac-man wedge. Temp card/test hooks (`__slice`/`__dbg`/`__testFruit`) **removed before ship**. Verified headless (DPR2): registry parses (**53**, first = slice-it/game), 0 console errors on toy+gallery, no 375px overflow, single-slice/combo(+bonus)/bomb-ends-game/miss all logic-tested, gallery search "slice" resolves the card (PNG decodes 1520²), card+OG legible. — Also still unpushed: **the Countdown custom date+time picker** (see the "New this round (052 Countdown picker)" note below). — Prior ✅ **PUSHED — housekeeping + 2 NEW "tools" → 052 Countdown to Anything + 051 Sleep Cycle Calculator (52 toys total).** Owner: "finish up the housekeeping… then build a couple new tools starting with the sleep cycle calculator." **Housekeeping:** (a) **stripped the redundant per-card tag chips** from the gallery (`createCard` in `main.js` no longer renders the `.tags`/`.tag` block — each card's only tag was its own slug, so the chips were noise; tags STAY in the registry + still power search via `buildTagSearchHay`, and the dead `.tags`/`.tag` CSS was removed — owner agreed "tags are good for a large gallery / other filters later, just don't show them now"); (b) **rewrote the stale `.ai/memory/project.md`** (it still described the pre-2026-06 "tool-shell/.panel/tool-cross" architecture that no longer exists → now reflects the current self-contained full-bleed `toys/<slug>/` + light/dark Geist `tools/<slug>/` family). **052 Countdown to Anything** (`tools/countdown/`, `utility`, **teal** `#0d9488`) — name any moment, watch **days/hours/minutes/seconds tick down LIVE** (`setInterval(render,1000)`); editable event title (`.meter__title` contenteditable); **dynamic presets** (New Year / Christmas / this weekend (Sat) / Friday 5pm via `nextAnnual(m,d,h)` + `nextWeekday(dow,h,m)` so they always roll forward); **share link** encodes name+target in the URL hash (`#e=…&t=…`, `readHash`/`writeHash`) so a friend opens your countdown; handles past dates (counts up). Default = New Year's Day. **051 Sleep Cycle Calculator** (`tools/sleep-cycle/`, `utility`, **night-blue**) — 90-min sleep-cycle math (`CYCLE=90, FALL_ASLEEP=15`): **Wake mode** (I'll sleep now / at X → best wake times = `base+15+n·90` for n∈{6,5,4,3}) + **Bed mode** (I must wake at X → best bedtimes = `base−15−n·90`); each option card shows cycle count + hours, and 5–6-cycle options get an `.opt--best` ★ideal badge. Both tools copy `tools/meeting-cost-meter/styles.css` recolored, **CSS-motif cards** (big themed `::after` number) + rendered OGs via og-gen `bignum()`. Registered everywhere (registry prepend 052→051, sitemap, NL tags `countdown-timer`/`sleep-calculator`, card rules + `:not()` chain, og-gen entries). Hub cache-bust **`styles.css?v=50`/`main.js?v=48`**. Verified headless: registry parses (**52**), no console errors (both tools + gallery + home), no 375px overflow, Countdown seconds tick live + presets recompute (183d to New Year, 176d to Christmas), Sleep math correct (bed 11pm→wake 8:15/6:45/5:15/3:45 AM; wake 6:30am→bed 9:15/10:45 PM), OGs legible, home featured auto-switches to Countdown (newest). ⏳ Owner to play-test both + `push`. — Prior **PUSHED `main d97a064`** was **3 NEW "tools":** **050 Time Is Money**, **049 Life in Numbers**, **048 The Latte Factor** — a trio of simple/fun single-purpose utilities in the **Meeting Cost Meter** family (owner: "3 simple but fun tool suggestions in line with the meeting cost meter" → "build all 3"). All live in `tools/<slug>/`, category `utility`, reusing the shared light/dark Geist "tool" design system (topbar + brand + theme toggle + odometer hero + glassy console) — I copied `tools/meeting-cost-meter/styles.css` and recolored per tool (money-green / cosmic-violet / coffee-amber). **050 Time Is Money** = your hourly wage made real: an "earning" tab ticks up what you've earned live since page-open (a personal meeting-meter), a "spending" tab turns any price into hours of your working life. **049 Life in Numbers** = enter a birthday → live-ticking seconds-alive hero + a stat grid (days, ~heartbeats, ~breaths, trips 'round the Sun, full moons, next-birthday countdown); born-on-a-weekday line. **048 The Latte Factor** = a daily-habit cost → 1/5/10/30-yr totals with a "invested at 7%" annuity toggle + emoji habit chips. Cards = **CSS-motif** (big themed number via `::after`, like the meeting meter — no PNG cards); OGs rendered via og-gen (`bignum()` helper). Registered everywhere (registry prepend newest-first 050→049→048, sitemap, 3 NL-phrase tags wage-timer/age-counter/savings-calculator, 3 card rules + `:not()` chain, 3 og-gen entries). Hub cache-bust **`styles.css?v=47`/`main.js?v=45`**. **⚠ Life in Numbers fix (owner: "numbers need to be responsive within card & viewport"):** the ~billion heartbeat/breath figures overflowed their grid cells → (a) `abbrev()` compacts them to `1.18B`/`245M`, (b) `.numcard` uses a **container query** (`font-size:clamp(1.15rem,19cqw,2.05rem)` + `overflow:hidden;white-space:nowrap`) so every stat scales to its own card, and the hero seconds counter got a smaller vw clamp — verified no overflow at 375px through a 96-yr-old's billions (`life-in-numbers/styles.css?v=2`). Verified headless: registry parses (50), no console errors on all 3 tools + gallery + home, no 375 overflow, home featured auto-switched to Time Is Money (newest); all 3 card motifs render. **PUSHED `main d97a064`** (owner said `push`); owner may still tweak copy/numbers/palettes by eye. **PUSHED: Steel Tongue Drum (047) VISUAL bug-fix + audio re-tune → `script.js?v=4`** (fixed the bowtie/triangle fill-clip + piano→handpan voice; card+OG re-rendered — see the detail just below). (Prior pushed `83ac2e4`: **047 Steel Tongue Drum** NEW `audio` premium 3D gunmetal dome + a **Kalimba refresh** brown→plum, dusk→night + a codified **AUDIO QUALITY BAR** — see the "⚠ Audio quality bar" section above + `.ai/memory/DECISIONS.md` + the "New this round (047…)" section below.) (Prior pushed `44a0951`: **046 Kalimba** — a playable glowing thumb-piano.) ⚠ **Audio still needs the owner's ears** — headless can't audition; owner may fine-tune the tongue-drum / kalimba voices by ear. **Tongue-drum `v=4` detail:** **Visual:** owner "I don't understand the white flashing angles" — each ring tongue was filled with a **plate-wide diagonal** light→dark gradient (clipped per capsule), so upper-left tongues painted as bright triangular wedges + lower-right ones as dark wedges = a confusing pinwheel of "white angles" that shifted as the strike-glow/ripples washed over. Fixed: replaced it with a gentle **cross-tongue emboss** (a soft cylinder cross-section aligned to each tongue's own light-facing flank → every tongue reads as a consistent raised rounded petal, no wedges); the strike-glow is now a **soft warm radial bloom clipped to the struck tongue** (was a flat additive wedge that blew toward white); ripples softened (alpha 0.5→0.3, 2.4→1.8px). **ROOT CAUSE (owner "these triangles seem like a broken filled shape"):** the `capsule()` helper — used as the CLIP for the emboss + glow fills — built a **self-intersecting bowtie** path (after the first end-cap it did `lineTo` to the *opposite* flank, `x2+nx·r` instead of `x2−nx·r`), so the clip region was two triangles meeting at the tongue's midline → the fills painted triangular artifacts. Rewrote `capsule()` as a proper two-arc stadium (`arc(x1,…,ph+90°→ph−90°); arc(x2,…,ph−90°→ph+90°); closePath` — arcs auto-join into the straight flanks). Tongues now read as clean rounded petals. (`slotPath`, the visible outline stroke, was always correct — only the fill-clip was broken.) **Card + OG re-rendered** to the cleaned-up dome. **Audio (was `v=3`):** owner said the v=2 voice "kinda sounds like a piano" (the clean harmonic ×2/×3/×4 partial stack + mallet = a piano/marimba spectrum). Re-tuned toward an authentic **steel tongue drum / handpan** voice: dropped the pure ×4, cut ×3 to a whisper, added an **inharmonic metallic ring cluster** (×2.76 / ×4.20 / ×5.40 / ×6.79 — bell-like, the "steel" shimmer), a **detuned-octave pair** (×2 and ×2·2^(7¢)) for a beating shimmer, an **attack pitch-glide "boing"** (~+11¢→pitch over 55ms on fundamental+octave, metal releasing tension), and a **darker/softer finger-mallet** (lowpass ≤850Hz, gain 0.03). Graph builds clean headless (DPR2, no exceptions) — but **needs the owner's ears** to confirm it now reads as a tongue drum, not a piano. Owner may still fine-tune the tongue-drum / kalimba audio by ear — audio can only be auditioned by the owner. (Prior pushed `ab194de`: **044 renamed Picture Puzzle → Slide Puzzle** + **NEW 045 Jigsaw Puzzle**.) (Prior pushed batch `d65a22f`: **4 physics/sim toys — 040 Double Pendulum, 041 Cloth & Tear, 042 Gravity Orbits, 043 Magnetic Face (Wooly Willy)**.) (Prior pushed batch: **4 physics/sim toys — 040 Double Pendulum, 041 Cloth & Tear, 042 Gravity Orbits, 043 Magnetic Face (Wooly Willy)** — owner picked all four via AskUserQuestion ("a couple more physics and sim toys"). See the "New this round (040–043)" section below. (Prior pushed: **039 Aurebesh Translator** (`toys/aurebesh-translator/`, category `visual`) — a live English→Aurebesh transliterator styled as a glowing **Imperial datapad** (the chosen aesthetic of 4 offered: datapad / holo projector / temple stone / cockpit HUD). Type Galactic Basic, read it back in the Star Wars alphabet glyph-by-glyph with a per-glyph **decode/scramble** animation; **hover/tap any glyph** → tooltip with its letter + canonical name (Aurek/Besh…); **Show letters** captions the Latin under each glyph; **Channel** cycles Imperial(cyan)/Rebel(amber)/Outer-Rim(green) accent recoloring the whole glow; soft synth **terminal bleeps** (pentatonic, iOS-unlock, toggle); **Copy link** shares the message **pre-encoded in the URL hash** (`#msg=…`) so a friend opens it already decoded. **Glyphs = a real canonical font, not freehand:** embedded `silvinor/font-aurebesh` (**OFL-1.1**, rendered from Stephen Crane's definitions), shipped **locally** in the toy folder (`aurebesh.ttf`) with `OFL.txt` bundled + a credit comment — stays self-contained, no CDN. Full detail in the "New this round (039 Aurebesh Translator)" section below. (Prior: `0f331b2` = **NEW: 038 Pottery Wheel** (`toys/pottery-wheel/`, category `wellness`) — pseudo-3D wheel-throwing in vanilla Canvas 2D: drag the spinning clay to shape a vase (surface of revolution), **Fire it** to kiln-glow + reveal a random glaze, then **paint** it from a 7-colour palette (wrap-around marks that band as the wheel turns). Full detail in the "New this round (038 Pottery Wheel)" section below. (Prior: `70f30c9` = **NEW: 037 Chladni Plate** (`toys/chladni-plate/`, category `simulation`) — a digital **cymatics** sand-vibration sim. Grains settle onto the nodal lines of a vibrating square plate (`A=sin(πmx)sin(πny)−sin(πnx)sin(πmy)`) via **node-seeking** (amplitude-scaled trial step kept only if it lands quieter — pure random jitter does NOT converge, verified). **Press/drag anywhere on the plate to pour sand** there → it migrates to the nearest nodal lines; endless via a recycling ring buffer (`MAX_PARTICLES`). Controls: **Slower/Faster** step a frequency ladder (finer figures), **Pause** freezes, **Fill** drops a full board (refresh), **Clear** empties — Fill + pour are **pause-aware** (sit frozen until Play). Constant `VIBE` shimmer so running looks alive vs frozen-when-paused. Tunables at top of `script.js` (`PARTICLE_COUNT 8000`, `FILL_COUNT 14000`, `MAX_PARTICLES 36000`, `POUR_FLOW 110`, `JITTER 26`, `M_MAX 6`, `VIBE 0.8`…). Went through heavy owner iteration (click-cycle→cursor-tune→control-bar→pour-anywhere→gentler pour+higher cap→Fill→pause-aware Fill); current toy `script.js?v=8`/`styles.css?v=2`. Registered everywhere + card ([3,5] figure) + OG; hub `styles.css?v=39`/`main.js?v=37`. (Prior: `6e496ae` = **NEW: 036 Pin Art** (`toys/pin-art/`, category `visual`) — a drawable digital metal **pinscreen** (vanilla Canvas; user's React/Tailwind prompt was adapted to the repo's vanilla stack with the user's explicit OK). Dense pin grid on charcoal; each pin's size+brightness scales by a smoothstep radial falloff to the cursor with a warm metallic highlight+shadow (3D relief). **Relief follows the hand on hover; press+DRAG inks a PERSISTENT impression** into a `base[]` grid (you draw shapes that stay) — `stamp()`/`stroke()` with path interpolation. **Erase** (button toggle / Shift-drag, subtractive) + **Clear** (`base.fill(0)`) controls replaced an earlier confusing click-to-freeze model (owner: "no way to draw, I don't get it"). Touch-ready. Feel knobs as labeled consts at top of `script.js`: `SPACING 40`, `RADIUS 150`, `R0 1.6`, `RPUSH 7.6`, `EASE 16`. No audio (visual fidget). Registered everywhere + card (drawn metallic loop) + OG; hub `styles.css?v=38`/`main.js?v=36`. Home featured spotlight auto-switched to Pin Art (newest). (Prior: `d541cd1` = **index.html landing-page hero + featured spotlight:** home had no front door (jumped into the grid) → added a **hero band** (eyebrow + h1 "Tiny toys for a spare minute." + value-prop lede w/ live count `#heroCount` + Browse-all / Surprise-me CTAs) and a **featured spotlight** (`#homeFeatured`) auto-driven by the **newest registry toy** (`newestTool` = first registry entry w/ a path; registry is newest-first → currently Perfect Circle); big preview + name/category/desc + "Play it →", GA `source: home_featured`. Grid header reframed "The Toys"→"Fresh picks"; stale "tags" meta copy fixed. JS: `renderHomeHero()`, `wireRandomButton` also wires `#heroSurprise`. `styles.css?v=37`/`main.js?v=35`. (Prior: `df20319` = **all-toys.html search/filter redesign:** removed the **Types accordion** (each toy's only tag was its own slug → it was a disguised list of every toy; the `TYPE_NL_PHRASES` keywords still power the search box, so no functional loss). Now: **hero search** (icon, focus glow, desktop autofocus, `/` hotkey, Esc clears); **category chips = primary filter** with a live count on each (`renderCategoryChips` tallies per-category); **persistent "Showing X of N"** count above the grid + Clear filters. `styles.css?v=36`/`main.js?v=34`; home `index.html` (`.tools__header`/`.tools-meta`) untouched. Open follow-up: per-card bottom tag chips are still just the slug (redundant now) — could strip/repurpose. (Prior: `0a6bb56` = **NEW: 035 Perfect Circle** (`toys/perfect-circle/`, category `game`) — owner wanted "a super addictive/viral one-page game"; draw a ring freehand in one stroke → scored on roundness (radius coefficient-of-variation + penalties for incomplete sweep / open gap; near-perfect ≈98%, can't trivially hit 100%). Per-segment **deviation heat-map** (green=ideal radius → red=bulged) over a dashed target ring, animated count-up %, confetti + flash on a new best, glowing live trail, deep-indigo world; **best in `localStorage` (`pc_best`)** shown in a HUD chip; synth audio (pen-ticks + score chime that brightens with score + new-best sparkle) w/ sound on/off toggle + iOS unlock. Registered everywhere + rendered card/OG; hub `styles.css?v=35`/`main.js?v=33`. (Prior: `ee97437` = **034 Rain on a Window → NIGHT scene** (`toys/rain-window/script.js?v=8`): darkened dusk→deep navy night (`#070d1a→#13233c`) so the city-light bokeh glow; **bokeh now slowly drift (elliptical sway) + gently twinkle** — `paintScene` split into `initBokeh()` (seed once) + `paintScene(time)` (animated), loop repaints scene + rebuilds the blurred frost on a throttle (`sceneAcc>=0.11`, ~9fps) so lights shimmer through the fog AND in wiped areas; frost veil softened 0.2→0.16 (wipe reveals focus not a dark drop); base bokeh alpha dimmed to 0.22–0.62 (soft orbs); re-rendered night card + OG. (Prior: `7ef035a` = **033 Fireworks audio/feel tuning round**) (`toys/fireworks/script.js?v=12`, `styles.css?v=2`): whistle rewritten **tone-dominant** (rising triangle + vibrato warble — was noisy hiss) then per owner raised to **2600→5400 Hz** and made **very faint** (gain 0.022), ~45% of launches; **Sound on/off toggle** (`#soundBtn`, top-center, mutes dry + reverb via one `outGain`); **~34% of shells finish with a delayed crackling-chrysanthemum burst** (`crackleField()` via the `pending` queue); **global outdoor reverb** (master wet send → big IR); **boom rebuilt all-noise + tanh saturation + AM rolling rumble**; silent ascent + faint launch thump; **blurry water reflection** for falling sparks near `WATER=H*0.84`; auto-launch interval widened to random **2.4–4.6s**; re-rendered multi-burst card. (Prior baseline below was `5d6f82d`: 033 Fireworks + 034 Rain on a Window pushed.) Earlier this session, post-031/032 polish (all pushed at 54e6f67): **Marble Drop** corner-stuck fixed (`script.js?v=3`; peg roll-off rolls toward open center near a side rail); **Kaleidoscope** stir+tap now visibly respond (`v=3`, cut self-drift) + tap fires on any non-drag click (`v=2`); **Aurora Drift** curtains ease in/out by fade not a hard pop (`v=2`, cull only once `peaked`); **Campfire** logs burn away (fuel model). Recent rounds: wellness 031/032; wellness 029/030 (`77fc8a6`); Chimp Test (017) "Full fun pass" (`067a42e`); toys 025–028 (`b78fab1`); baseline `94a631c` (24 toys).

### New this round (housekeeping + 052 Countdown + 051 Sleep Cycle) — BUILT, ⏳ awaiting push (52 toys)
- **Housekeeping (owner: "finish up the housekeeping, that seems pretty quick"):**
  - **Stripped per-card tag chips** — `createCard()` in `assets/main.js` no longer renders the `.tags`/`.tag` chip block at the bottom of each gallery card. Rationale: each toy's only tag was its own slug, so the chips were a redundant restatement of the title. **Tags REMAIN in `tools-registry.json`** and still feed search (`buildTagSearchHay` + `TYPE_NL_PHRASES`), so filtering is unaffected — owner explicitly agreed: "tags per toy is good for when we have a large gallery and need other ways to filter; we don't need to show them now." Removed the now-dead `.tags`/`.tag` rules from `assets/styles.css`.
  - **Rewrote `.ai/memory/project.md`** — it was pre-2026-06 and described a long-gone architecture (a shared "tool-shell" with `.panel`/`tool-cross`/`tool-header` chrome). Replaced with an accurate current-state description: a branded launcher hub + self-contained full-bleed `toys/<slug>/` Canvas experiences + the light/dark Geist `tools/<slug>/` utility family; the add-a-toy pipeline; the design/audio quality bars. (HANDOFF.md remains the authoritative live bridge; project.md is now at least no longer misleading.)
- **052 Countdown to Anything** (`tools/countdown/`, category `utility`, palette **teal** `#0d9488`) — owner asked for a couple new tools after the sleep calculator; this is the second. Name any moment and watch the **days / hours / minutes / seconds tick down live**. Big teal days-hero (`.odo`, `clamp(2.8rem,13vw,6.4rem)` — reused the sleep-cycle sizing) + an H/M/S card grid, refreshed every second by `setInterval(render,1000)`. **Editable event name** (`.meter__title` is `contenteditable`, writes back to the hash on input). **Target** = a `datetime-local` input. **Dynamic presets** that always roll forward: New Year (`nextAnnual(0,1,0)`), Christmas (`nextAnnual(11,25,0)`), This weekend (`nextWeekday(6,0,0)` = next Saturday 00:00), Friday 5pm (`nextWeekday(5,17,0)`). **Shareable:** name + target ISO are encoded in the URL hash (`#e=<name>&t=<iso>`); `readHash()` restores on load, `writeHash()` updates on every edit, and "Copy share link" copies the full URL → a friend opens your exact countdown. Past dates count **up** ("… ago"). Default target when no hash = New Year's Day. `script.js?v=2`/`styles.css?v=2`.
  - **⚠ Custom date+time picker (follow-up, owner picked "finish up 1" = give Countdown the same treatment Sleep Cycle got):** the native `datetime-local` had the identical dark-on-dark calendar icon + unthemeable OS popup. **Replaced with a custom on-brand picker** (`.dtpick`): pill trigger with an accent calendar SVG (`currentColor`) + a compact "Jan 1, 2027 · 12:00 AM" label, opening a themed popover (`.dtpop`) with a **month calendar** (`‹ Month YYYY ›` nav, weekday row, 6-week day grid, muted adjacent-month days, `--today` ring, accent-selected day) **plus the same hour/min(5)/AM-PM time columns** from Sleep Cycle. The old `#target` is now a **hidden input** still holding the `YYYY-MM-DDTHH:mm` string, so presets / share-hash / `render()` / `markPreset()` are all unchanged — the picker just `commit()`s to it (and `syncPicker()` pulls preset/hash changes back into the trigger + calendar). Closes on outside-click (`pointerdown` capture), Escape, or toggle; `.dtpop[hidden]{display:none}` included from the start (the bug that bit Sleep Cycle). Verified headless over HTTP (both themes + 375px): opens/hidden-on-load correct, month nav works (Jan→Feb 2027), day-15 + 8:45 AM pick → trigger "Feb 15, 2027 · 8:45 AM" + hidden `2027-02-15T08:45` + days-hero recomputes (228), preset click re-syncs the trigger (Christmas → "Dec 25, 2026 · 12:00 AM", aria-pressed), all close paths work, 0 console errors, no horizontal overflow. ⏳ **BUILT, awaiting `push` (only `tools/countdown/*` changed — no hub cache-bump needed).**
- **051 Sleep Cycle Calculator** (`tools/sleep-cycle/`, category `utility`, palette **night-blue**) — owner: "build a couple new tools starting with the sleep cycle calculator." 90-minute sleep-cycle math. Constants `CYCLE=90, FALL_ASLEEP=15, CYCLES=[6,5,4,3], IDEAL={5,6}`. **Two modes:**
  - **Wake mode** ("I'm going to sleep now / at a set time" → best times to **wake up**): `wake = base + FALL_ASLEEP + n·CYCLE` for each n → wake at the END of a full cycle so you're not jolted mid-cycle.
  - **Bed mode** ("I have to wake at X" → best times to **go to bed**): `bed = base − FALL_ASLEEP − n·CYCLE`.
  - Each result is an option card showing the time + its **cycle count and total hours**; the 5- and 6-cycle options (≈7.5–9 h) get an `.opt--best` ★ "ideal" badge. Verified: bed 11pm → wake 8:15 / 6:45 / 5:15 / 3:45 AM; wake 6:30am → bed 9:15 / 10:45 PM (etc.). `script.js?v=2`/`styles.css?v=2`.
  - **⚠ Custom time picker (owner: "the clock icon is too dark and the time picker is really ugly"):** the first pass used a native `<input type="time">` — in dark mode its `::-webkit-calendar-picker-indicator` clock is near-invisible dark-on-dark, and the browser's native time popup (blue/white OS grid) can't be themed. **Replaced entirely with a custom on-brand picker** (`.timepick`): a pill trigger showing an inline **clock SVG in `currentColor`/accent** (always visible) + the big "9:30 PM" value, and a themed **three-column popover** (`.timepop` → hour 1–12 / minute 0–55 by 5 / AM-PM as `.timeopt` buttons, accent-highlighted selection, scroll-selected-into-view on open). Closes on outside-click (`pointerdown` capture), Escape, or re-toggle; the **Now** button snaps to the current time rounded to nearest 5 min. State is `curH`/`curM` (24-hr) driving `inputMins()`; no native input remains. `sleep-cycle styles.css?v=3`/`script.js?v=2`. **⚠ Follow-up bug (owner: "the picker is open all the time and I can't close it"):** the popover toggles via the `hidden` attribute, but `.timepop{display:flex}` (author rule) **overrode the UA `[hidden]{display:none}`** → it was ALWAYS visible. Fixed with an explicit `.timepop[hidden]{display:none}` rule (`styles.css?v=3`). The first headless pass missed it because it checked the `hidden` *attribute*, not *computed visibility* — re-verified with `getComputedStyle().display` + rect height: hidden on load, visible after open, hidden after both outside-click and Escape. Verified headless (DPR2, both themes): opens/selects/recomputes hero (9:30 PM → wake 6:45 AM), 0 console errors, no 375px overflow; screenshots confirm a clean themed dropdown in light + dark.
- **Both tools:** copied `tools/meeting-cost-meter/styles.css` and recolored (sed swap of the meeting-meter red palette → teal / night-blue). Cards are **CSS-motif** (a big themed number via `.card__preview[data-slug]::after`, matching the rest of the tool family — no PNG card). OGs rendered via `scripts/og-gen.html` using the `bignum(text,sub,g1,g2,g3)` helper (Countdown = teal "12 days", Sleep = its motif). Registered everywhere: tools-registry.json (prepended newest-first — countdown then sleep-cycle at the very top), sitemap.xml, `TYPE_NL_PHRASES` (`countdown-timer`: countdown/timer/new-year/christmas/deadline/…; `sleep-calculator`: sleep/bedtime/wake/90-minute-cycle/…), 2 card rules + `:not()` chain (styles.css), 2 og-gen entries. Hub cache-bust bumped **`styles.css?v=50`/`main.js?v=48`** (index.html + all-toys.html). Home featured spotlight auto-switches to Countdown (newest registry entry).
- **Verified headless (DPR2):** registry JSON parses (**52 toys**, first = countdown/utility); no console errors on either tool + gallery + home; no 375px horizontal overflow on either tool; Countdown seconds tick live (watched 15→13) and all 4 presets recompute correctly (183 days to New Year, 176 to Christmas at capture time); Sleep math verified in both modes (see times above); both OG images render legibly (viewed `assets/og/countdown.png` — "Countdown to Anything", teal "12 days", No. 052). ⏳ Owner to play-test feel/copy/palettes then `push` (exclude both `.claude/settings*.json` as always).

### New this round (047 Steel Tongue Drum + Kalimba plum/night refresh) — PUSHED `main 83ac2e4`, 47 toys live (`.claude/settings*.json` left out as always)
- **047 Steel Tongue Drum** (`toys/tongue-drum/`, category `audio`) — owner: "do another audio toy. This one a steel tongue drum," then two polish rounds: "still seems a bit off, both visually and audibly… needs to sound amazing… maybe reverb? maybe tuning?" A warm, meditative **tank drum** played top-down. **Strike a tongue (tap) or drag across them for a run**; the struck tongue glows warm-amber and sends a **ripple** across the drum (clipped to the disc). Sector-based hit test (forgiving on mobile). Controls: **Scale** (Minor / Major / **Akebono** pentatonics — recomputes tuning), **Labels** (light note names), **Sound**. Center = root (A3), ring of 8 ascends around (9 notes). Calm dark teal-slate scene + faint dust.
  - **Visual (upgraded to a premium look, `script.js?v=2`):** a first pass was a flat bright-chrome disc that "looked like a hubcap." Reworked to a **convex 3D gunmetal DOME** — radial body gradient `#7d8894→#525c66→#333b43→#171c22` offset upper-left, a broad soft **sheen**, a warm lower-right **scene glint**, faint **concentric lathe marks**, a **raised bezel rim** (light top-arc / dark bottom-arc). Tongues are the same gunmetal, outlined by an **embossed rounded slot-cut** (dark core + shadow lip down-right + bright highlight lip up-left, light dir `LX,LY=-0.71`) with a drilled tip hole; central tongue = an embossed ring-cut with a bridge gap. Dark body makes the amber strike-glow pop.
  - **Audio (upgraded per the AUDIO QUALITY BAR — see below):** a tongue drum is *tuned* → largely **harmonic + bell-like**. `tone(freq,vel,pan)` = **soft-mallet attack** (10–14ms) fundamental + **+3¢ chorus** + **strong octave ×2** (0.17, the hang body) + **twelfth ×3** + **double-octave ×4** sparkle + a whisper of **inharmonic ×2.76** + a low **body "tonk"** (×0.5) + a lowpassed-noise felt-mallet thump; **long blooming sustain** (`6.0·(196/f)^0.42`, 2.4–7.0s). **Stereo-spread per tongue**; bus → **compressor** → master lowpass. **Space:** big lush **convolver reverb** (impulse 4.2s, wet 0.44, `makeImpulse` now low-passes its noise for a smooth tail + a **highshelf shimmer**, highpass 170) **plus a spacious feedback delay** (0.33s, fb 0.34, lowpassed) that feeds BOTH the output and the reverb so repeats bloom into the hall. iOS unlock. Tunables: `RING=8, BASE_MIDI=57, SCALES`. Theme `#0a1216`. `styles.css?v=1`.
  - Registered everywhere (registry prepend `audio`, sitemap, NL, card rule bg `#0a1216` + `:not()`, og-gen No.047 `AUDIO · INSTRUMENT` accent `#ffce8c`). **Real rendered card** `assets/cards/tongue-drum.png` (the dark 3D dome rosette) + **OG** (re-rendered, legible). ⚠ **Audio still needs the owner's ears** — headless can't audition; graph builds clean.
- **Kalimba refresh (046):** owner "make the kalimba not brown" → chose **Deep plum** (of 4 offered) → body gradient `#4a2d63→#331d47→#1e1233` (grain + soundhole retinted plum; kept the warm gold glow rim + sheen). Then owner "compliment the kalimba with a sky that is also night themed" → sky changed **dusk (warm gold horizon) → night** (`#070818→#12123a→#241d48→#3a2b55`) with a **twinkling star field** (`seedStars`, density by area, upper 70%) + a **soft glowing moon** (upper-right, halo + craters), and the drifting ambient motes cooled to blue-white; the warm play-bloom is kept (warm instrument light against cool night = the look). Kalimba `theme-color`/body bg/card bg → `#070818`, og-gen bg → night, accent → `#cdd6ff`. **Card + OG re-rendered** (plum kalimba under stars/moon). `script.js?v=3`.
- Hub cache-bust bumped **`styles.css?v=46`/`main.js?v=44`** (index.html + all-toys.html). Home featured spotlight auto-switches to Steel Tongue Drum (newest registry entry).
- **Verified headless (DPR2):** registry parses (**47 toys**, first = tongue-drum/audio); no console errors (both toys + gallery); no 375px overflow; tongue-drum renders the slot-cut rosette + strikes/ripples, kalimba renders plum body + stars + moon; both gallery cards' `background-image` resolve. Audio graphs build + strikes/glissando run with no exceptions — **but both instruments' sound still needs the owner's ears** (headless can't audition). ⏳ Owner to play + audition audio, then `push`.

### New this round (046 Kalimba) — PUSHED `main 44a0951`, 46 toys live (`.claude/settings*.json` left out as always)
- **046 Kalimba** (`toys/kalimba/`, category `audio`) — owner: "I want to do a kalimba like the game Sky Children of Light." A **playable glowing thumb-piano** in that dreamy *Sky* spirit (public name kept generic **"Kalimba"** — trademark-safe, like Wooly Willy→Magnetic Face). Warm dusk sky (violet→rose→gold gradient + a warm bloom behind the instrument that **brightens as you play**, drifting light motes), a glowing **wooden soundboard** (grain, sheen, soundhole, metal **bridge/pressure bar**), and **15 steel tines** in the authentic **center-out kalimba arch** (center = longest = lowest). **Pluck a tine** to play; **swipe across for a glissando** (band-based hit test = each tine is a full-width vertical tap zone, so even thin tines are easy on mobile). Every note: the tine **wobbles** (sampled bend, tip sways most), the **tip flares gold** with a halo ring, and a **rising mote of light** floats up (the *Sky* signature). Controls: **Key** (cycles C/D/F/G/A pentatonic roots — recomputes freqs + names), **Labels** (note names above each tip), **Sound**.
- **Tuning:** major-pentatonic (`DEGREES=[0,2,4,7,9]`) so anything sounds consonant; **center-out pitch order** (`order` array places lowest at center, alternating right/left outward → real kalimba layout, verified via Labels: center C4, edges up to A6/G6). All-MIDI math (`freq = 440·2^((midi−69)/12)`, names via `NOTE_NAMES`).
- **Synth (Web Audio, no samples) — REBUILT for quality (owner: "the audio needs to sound really great").** Physically grounded on a kalimba tine = **clamped-free steel bar** (INHARMONIC overtones ≈ **1 : 6.27 : 17.55**, bright metallic "ting" on attack over a warm fundamental). `tone(freq,vel,pan)` is additive: **body** = sine fundamental (0.30) + a **+4-cent detuned sine** (0.16, chorus shimmer) + faint **triangle edge** (0.05) + **octave** sine ×2 (0.05), all with a subtle **attack pitch-glide** (start 1.2% sharp → f0 over 30ms, like a released tine); **metallic bar modes** = fast-decaying sines at ×6.27 (0.10, ~0.14s) and ×17.55 (0.032, ~0.06s) — this is the tine sparkle; **pluck contact** = 12ms bandpassed-noise tick. Per-note **duration scales with pitch** (`3.2·(261.63/f)^0.5`, 0.9–4.0s → lows ring longer). Partials skip above 0.9·Nyquist. **Stereo spread**: each tine panned by its board position (`(idx/(N−1)−0.5)·0.6`) via StereoPanner → wide immersive field. **Bus:** all voices → dry(0.9) + wet(0.27) sends; wet = **pre-delay(20ms) → highpass(250) → convolver** (synth impulse `makeImpulse(2.6,2.4)`, keeps the lush tail clean); dry+wet → **DynamicsCompressor** (glue + anti-clip on glissandos) → master lowpass(11k, silk) → `outGain`(mute) → destination. iOS silent-buffer unlock. Verified headless (DPR2): taps + a fast glissando build the graph and run with **no exceptions**; the compressor guards clipping — but **the actual sound still needs the owner's ears** (headless can't audition). Tunables at top: `N=15, DEGREES, ROOTS, VIB_DECAY=6.5, MAX_DISP=11`. Theme `#1a1636`. `script.js?v=2`/`styles.css?v=1` (mobile: hint lifted to `bottom:58px` so it clears the bottom-left back-link).
- **Registered everywhere:** tools-registry.json (prepended top, newest-first, category `audio`), sitemap.xml, TYPE_NL_PHRASES (main.js; kalimba/thumb-piano/mbira/pentatonic/sky/calm/glissando…), card rule bg `#1a1636` + `:not()` chain (styles.css), og-gen entry (No. 046, `AUDIO · INSTRUMENT`, accent `#ffcf8a`). **Real rendered card** `assets/cards/kalimba.png` (a plucked, glowing kalimba with rising motes + halo, square crop of the board on the dusk sky) + **OG** `assets/og/kalimba.png` (verified legible). Hub cache-bust bumped **`styles.css?v=45`/`main.js?v=43`** (index.html + all-toys.html). Home featured spotlight auto-switches to Kalimba (newest registry entry).
- **Verified headless (DPR 2):** no console errors (toy + gallery); no 375px overflow (15 tines fit with clear arch); rest/play/labels all render correctly; the play state lights the plucked tine gold + spawns rising motes + blooms the scene; gallery search "kalimba" surfaces the card and its `background-image` resolves to `kalimba.png`; registry parses (**46 toys**, first = kalimba/audio). ⏳ Owner to play + **audition audio** (mbira warmth, reverb amount, glissando feel, per-key) then `push`.

### New this round (Slide-Puzzle rename + 045 Jigsaw Puzzle) — PUSHED `main ab194de`, 45 toys live (`.claude/settings*.json` left out as always)
- **044 renamed Picture Puzzle → Slide Puzzle** (owner: "call picture puzzle slide puzzle because that is what it is"). Since nothing was pushed yet, did a **full slug rename** `picture-puzzle`→`slide-puzzle` for consistency: folder `toys/slide-puzzle/`, assets `assets/cards/slide-puzzle.png` + `assets/og/slide-puzzle.png` (**OG re-rendered** so its text reads "Slide Puzzle"), and every reference updated — registry (slug/name/tags/path), sitemap, TYPE_NL_PHRASES key (dropped "jigsaw" from its keywords since jigsaw is now its own toy), `data-slug` card rule + `:not()` chain (styles.css), og-gen entry (name/key/motif), and all the toy's own index.html meta/JSON-LD/frame-corner + script.js comment. Only leftover "picture-puzzle" string is in `.claude/settings.local.json` (never committed). The toy itself is unchanged (still the sliding-tile 15-puzzle: random procedural picture, Picture/Pieces 3×3-4×4-5×5/Scramble/Peek/Sound, win → "Solved in N!" + confetti + green border). Theme `#10131c`.
- **045 Jigsaw Puzzle** (`toys/jigsaw-puzzle/`, category `game`) — owner: "That's a good addition to the gallery but I was thinking jigsaw puzzle so let's make that another toy." A **real interlocking-piece jigsaw**, genuinely distinct from the slide puzzle: pieces have classic **tab/blank edges** and are **scattered around the tray**; you **drag each one home** and it **snaps into place** when released within `cell*SNAP_FRAC` (0.42) of its slot. Reuses the same 7 procedural picture generators (self-contained, no external image). Controls: **Picture** (cycle scene → new puzzle) / **Pieces** (3×3→4×4→5×5) / **Shuffle** (re-scatter) / **Peek** (faint 0.28-alpha ghost of the scene in the tray) / **Sound** (synth: soft sine *lift* on grab, woody bandpass-noise+triangle *snap* on placement, pentatonic *win chime*; iOS unlock, `outGain` mute). Chip shows placed count `N / total` → "Solved!" (green) on completion; win → confetti + "Solved!" banner + green glowing tray border. **The finished picture keeps its piece seams** (authentic jigsaw look, not a flat swap).
  - **Edge geometry (the crux):** a normalized jigsaw profile `JIG` (mushroom-tab bezier control points, x along 0..1 / y perpendicular 0..1) is mapped per edge via `edgePath(ax,ay,bx,by,sign)` — `sign` ∈ {+1 tab out, −1 blank in, 0 flat border}, tab height `TAB_H=0.26` of a cell, outward normal `(uy,−ux)`. Seams: `vSeam[r][c]`/`hSeam[r][c]` random ±1 shared between neighbours (one's tab = other's blank), borders forced flat. Each piece is drawn by **clipping to its path then blitting the WHOLE source image** offset so the piece's home cell lands at its current position (`drawImage(srcCv,0,0,S,S, X−col*cell, Y−row*cell, cell*N, cell*N)`) — so tabs correctly show neighbour imagery. Hit-testing uses `ctx.isPointInPath` on the rebuilt path (topmost-first). Floating pieces get a drop-shadow (solid base fill first so the shadow reads), placed pieces sit flush and are unshifted to the front of the draw array (under floaters). Tunables: `S=900, SIZES=[3,4,5], TAB_H=0.26, SNAP_FRAC=0.42`. Theme `#12151f`. `script.js?v=2`/`styles.css?v=1`.
  - ⚠ **CRITICAL bug fixed (owner: "the jigsaw puzzle just isn't working"):** `ctx.isPointInPath()` ignores the current transform and expects **device pixels**, but `pieceAt()` was passing raw CSS-pixel `clientX/clientY` → on any **high-DPI/Retina screen (DPR>1) NO piece was grabbable** (the whole toy felt dead). Headless verify had missed it because it ran at `deviceScaleFactor:1` (DPR=1, where CSS px == device px). Fix: `pieceAt` now hit-tests at `x*DPR, y*DPR`. Re-verified the **full win path at `deviceScaleFactor:2`** (drag every piece home → solved). `script.js` bumped `?v=1`→`?v=2`. (Slide Puzzle is unaffected — it hit-tests with plain rectangle math, not `isPointInPath`.)
  - **Card/OG built via a temporary `window.__card(leave,pic)` hook** (places all pieces home, lifts the last `leave` pieces above their empty notches for a mid-solve composition) — **hook since REMOVED** from the shipped script. Card `assets/cards/jigsaw-puzzle.png` = a 4×4 sunset mid-solve with 3 pieces hovering over their holes (unmistakably a jigsaw); OG `assets/og/jigsaw-puzzle.png` (og-gen `#jigsaw-puzzle`, No. 045, `GAME · PUZZLE`, accent `#8ad0ff`, reuses card motif) — both verified legible.
- **Registered (045):** tools-registry.json (prepended top, newest-first, category `game`), sitemap.xml, TYPE_NL_PHRASES (main.js; jigsaw/interlocking/tab-blank/assemble/etc.), card rule bg `#12151f` + `:not()` chain (styles.css), og-gen entry. Hub cache-bust bumped **`styles.css?v=44`/`main.js?v=42`** (index.html + all-toys.html). Home featured spotlight auto-switches to Jigsaw Puzzle (newest registry entry).
- **Verified headless:** registry JSON parses (**45 toys**, first = jigsaw-puzzle); no console errors (gallery + both toys); no 375px overflow; jigsaw renders correct interlocking pieces at 3×3 & 4×4, Peek ghost works; **win path confirmed** — a scripted drag of every piece home → `solved:true`, chip "Solved!" green, full picture + green border + confetti rendered; gallery search "jigsaw" surfaces the card and its `background-image` resolves to `jigsaw-puzzle.png`; temp hooks confirmed gone. ⏳ Owner to play-test both (slide feel; jigsaw drag/snap feel, scatter tidiness, difficulty at 5×5, audio) then `push`.

### New this round (040–043, physics/sim batch) — PUSHED `main d65a22f`, 43 toys live (`.claude/settings*.json` left out as always)
Owner: "let's add a couple more physics and sim toys" → offered 4 via AskUserQuestion, owner picked **all four** (and specified the magnet one be a **Wooly Willy**). All vanilla Canvas-2D, self-contained, full SEO/OG. Standing constraint honored: only push on owner's word, `.claude/settings.json` left out.
- **040 Double Pendulum** (`toys/double-pendulum/`, category `simulation`) — faithful chaotic double pendulum. Exact equations of motion integrated with **RK4** (`deriv()`/`step()`), `SUBSTEPS=8`/frame, near-frictionless `DAMP=0.99997` (an earlier `0.9992` killed 90% of energy over the card capture — fixed). Chrome + brass bobs (radial-gradient + specular), metallic rods, pivot mount; the outer tip paints a **hue-cycling glowing trail** in an offscreen buffer (`TRAIL_FADE=0.028`, wide soft glow + bright core). **Drag either bob to place it; releasing after a drag FLINGS it** — `pmove` tracks the dragged joint's smoothed angular speed (rad/s), `pup` converts to internal omega (`flingW*FLING/30`, clamped ±1.2) applied to w1 (inner→whole) or w2 (outer). (Added per owner "I want to be able to fling the double pendulum"; needed a local `now()` helper — its absence threw "now is not defined", fixed.) Controls: New swing / Trails toggle / Sound (subtle bandpass-noise swish gated by |w1|+|w2|). Tunables at top. `script.js?v=1`.
- **041 Cloth & Tear** (`toys/cloth/`, category `simulation`) — a hanging cloth on a **verlet** grid (`COLS=38×ROWS=26`, `GRAV=0.34`, `ITER=4`), pinned at every 3rd top point (scalloped swags). Rendered as flat-shaded **"shot-silk" quads** (fold shading from per-cell compression, 3-stop ramp shadow→magenta→gold). **Drag mode** grabs the nearest point; **Tear mode** (or a fast swipe in Drag mode, or over-stretch past `TEAR_LEN=3.4×`) cuts links via `hEdge/vEdge` alive-flags → real holes (a cell draws only if its 4 bounding links survive). **Wind toggle** (owner: "the wind is too strong" → dialed the horizontal push `0.9→0.32`, flutter `0.12→0.05`). Soft synth rip on tear. Controls: Reset / Mode / Wind. `script.js?v=1`.
- **042 Gravity Orbits** (`toys/gravity-orbits/`, category `simulation`) — a gravity sandbox: massive central star + softened star-dominant & mutual gravity (`STAR_K=1500`, `PL_K=90`, `SOFT=20`), semi-implicit Euler, `SUB=2`. **Drag out from anywhere → release to fling a planet** (aim arrow while dragging); planets fall into orbit, trace glowing trails (offscreen buffer), slingshot, **merge** (momentum-conserving) and get **swallowed** by the star (flare + `swallow()` tone). 3 seeded orbits on load/Reset. Controls: Reset / Trails / Sound (whoosh on fling, clink on merge). ⚠ **CRITICAL BUG fixed:** owner "the stretch to fling and speed is too high" → added a global time-step tunable named `H`, but **`H` collided with the `H = window.innerHeight` variable** — resize() set it to 900 → every planet flung off-screen ("now there are no orbs"). **Renamed to `TSTEP=0.6`** (calmer) + `VSCALE 0.09→0.05` (gentler fling). Now orbits are stable and calm. `script.js?v=1`.
- **043 Magnetic Face** (`toys/wooly-willy/`, slug `wooly-willy`, category `game`) — a **Wooly Willy**: a charming bald cartoon face (peach head, dot eyes w/ highlights, rosy cheeks, bulbous nose, red smile, ears) on a cream red-bordered card under a glass sheen, with **iron filings** (`N=3400`) heaped in a bottom reservoir. **Drag the magnetic wand** — filings within the field are attracted to it (pure attraction `PULL`, radial `fang` alignment = authentic field-line look) and comb into hair/beard/brows/moustache; they stay where left (heavy `FRICTION`). **Shake it off** re-heaps them. Owner iterated the magnet strength (too strong): `PULL 4.2→2.7→**1.5**`, `R_FRAC→0.115`. Named "Magnetic Face" publicly (trademark-safe); slug + NL search keep "wooly willy". Soft synth shimmer while dragging. `script.js?v=1`.
- **Registration (all 4):** tools-registry.json (prepended newest-first: wooly-willy, gravity-orbits, cloth, double-pendulum), sitemap.xml, TYPE_NL_PHRASES (main.js), 4 card rules + `:not()` chain (styles.css), 4 og-gen entries. **Rendered card thumbnails** (`assets/cards/*.png`) + **per-toy OG** (`assets/og/*.png`). The **wooly card** needed a temporary `window.__hairdo()` hook to place filings into a full hairdo directly (mouse-drag sims couldn't reliably carry filings to the crown) — **hook since removed** from the shipped script. Hub cache-bust bumped **`styles.css?v=42`/`main.js?v=40`** (index.html + all-toys.html).
- **Verified headless:** all 4 registry JSON parses; no console errors (gallery + each toy); no 375px overflow; all 4 card CSS rules resolve (`data-slug` → correct png); gravity orbits stable after the TSTEP fix; interactions work. Home featured spotlight will auto-switch to Magnetic Face (newest registry entry). ⏳ Owner to play-test feel (pendulum fling strength, cloth tear/wind, gravity fling+speed, magnet strength) + audition audio, then `push`.

### New this round (039 Aurebesh Translator) — PUSHED `main efcbebb`, 39 toys live (`.claude/settings.json` left out as always)
- **039 Aurebesh Translator** (`toys/aurebesh-translator/`, category `visual`) — type in **Galactic Basic (English)** and it transliterates **live** into **Aurebesh** (the Star Wars writing system) on an **Imperial datapad**: brushed-metal device (corner screws, header cog + "IMPERIAL DATAPAD" + status dots + blinking "TRANSMITTING"), a dark **CRT screen** (scanlines, vignette, occasional flicker, inner accent glow) showing the glowing Aurebesh, the English **echo** dim underneath, and a console **input line** (`>` prompt). Pre-fills with "May the Force be with you" so it's alive on arrival.
- **Glyph source decision (important):** I did NOT freehand 26 glyphs (too risky for a designer's eye). Instead embedded a **canonical Aurebesh font** — `silvinor/font-aurebesh`, **OFL-1.1**, author rendered **Stephen Crane's original definitions** verbatim → faithful. Used the **No-Ligatures** variant so it's a clean **1 Latin char ↔ 1 glyph** mapping (needed for per-glyph spans / decode animation / hover-reveal; the ligature variant would merge ch/sh/th/etc. and break that). Shipped **locally**: `toys/aurebesh-translator/aurebesh.ttf` + `@font-face` (no CDN → self-contained), with `OFL.txt` bundled in the folder + a credit comment in `styles.css` (OFL requires the license to travel with the font). *Open future option the owner was offered: an authentic **ligatures mode** (ch/sh/th/ae/eo/kh/ng/oo render as their special single glyphs) using the ligature variant on a single text run.*
- **Interactions** (clears the "every toy must be interactive" rule): **type** → live translate, each new glyph runs a short **scramble/decode** (cycles random Aurebesh ~5×34ms then locks) + blur/opacity fade-in (only the changed tail re-animates, via common-prefix diff vs previous string); **hover a glyph** (desktop) or **tap** (mobile, 1.5s) → fixed tooltip "`LETTER` · Name" (full Aurebesh letter-name map A–Z in `script.js`); **Show letters** toggle → Latin caption under every glyph (`.out.is-reveal .glyph::after{content:attr(data-letter)}`); **Channel** button cycles 3 accent worlds **Imperial #38e1ff / Rebel #ffb347 / Outer Rim #54ffb0** (sets `--accent` + `--accent-rgb` on `:root`, recolors all glow); **Sound** toggle = soft synth **terminal bleeps** (triangle+sine through lowpass, pentatonic by char-code, gain ~0.05, iOS silent-buffer unlock on first gesture); **Copy link** = copies `…/index.html#msg=<encoded>` and on load `decodeHash()` pre-fills from `#msg=` → **send a friend a secret message that opens already decoded** (shareable/viral angle). Reduced-motion: skips scramble/fade.
- **Verified headless** (Playwright): no console errors (toy + hub), `document.fonts.check('"Aurebesh"')` true, typing updates echo + correct glyph counts, reveal/accent/tooltip/copy all work, **no horizontal overflow @375px** (fixed a mobile bug where the bottom-left back-link collided with "No. 039" — on ≤430px the hint is now hidden and the back-link stays bottom-left). **Real rendered card** `assets/cards/aurebesh-translator.png` (datapad showing "Star Wars" in glowing cyan Aurebesh, overlays hidden, ~square crop of `#datapad`) + **OG** `assets/og/aurebesh-translator.png` (og-gen `#aurebesh-translator`, No. 039, accent `#38e1ff`, motif reuses the card). Hub gallery picks up the card; search surfaces it for `jedi`/`decode`/`aurebesh`.
- Registered everywhere: tools-registry.json (prepended, newest-first), sitemap.xml, `TYPE_NL_PHRASES` (main.js; star-wars/jedi/sith/decode/cipher/galactic-basic/etc.), card rule + `:not()` chain (styles.css). Hub cache-bust bumped **`styles.css?v=41`** + **`main.js?v=39`** (index.html + all-toys.html). Toy-local assets `script.js?v=1`/`styles.css?v=1` (fine, never deployed elsewhere). Home featured spotlight auto-switches to Aurebesh Translator (newest registry entry).
- **Follow-up — Transmit / "speak the phrase" (owner asked "can the datapad speak the phrase?", then "I imagined it in Aurebesh, not English"):** added a **Transmit ►** control + a **Voice** selector. First pass (`319964f`, `script.js?v=2`) read the underlying English via Web Speech; owner wanted it to *sound Aurebesh*, so the final design (`c5e1a3e`, `script.js?v=3`) **drops English entirely** and offers **two user-selectable Aurebesh voices** (owner chose "let the user pick — Letter names or Droid chatter"):
  - **Letter names** — recites each glyph's NAME (Aurek/Besh/Cresh…, full map in `NAMES`) via Web Speech, **one chained utterance per glyph** (`speakNames()`, each `onend`→next) so the glyph lights in **perfect sync**; pitch 0.6 / rate 0.95 + `pickVoice()` prefers a deeper/synthetic system voice (Zarvox/Trinoids…) for an alien cadence. (Digits speak the digit; punctuation/spaces skipped.)
  - **Droid** — **wordless astromech chatter**, a synthesized chirp per glyph (`droidBeep()`: square/saw osc, exponential pitch-glide up/down by char-code, vibrato LFO, bandpass; `speakDroid()` steps ~150–220ms/glyph), pure **Web Audio** (no TTS), glyphs pulse along.
  - **KEY architecture note:** Aurebesh is a *script*, not a spoken language (no canonical pronunciation) — that framing is why there are voice *modes* at all. **Droid needs no `speechSynthesis`** (pure synth), so Transmit only hides if the browser has **no audio at all** (`AudioContext` absent); the **Voice button hides** when only one mode is available (no TTS → droid-only). `VOICES = synth ? ["names","droid"] : ["droid"]`, default **Letter names**.
  - Shared: `.screen.is-transmitting` glows via a `txglow` pulse; a bandpassed-noise radio **key-blip** in/out (`commsBlip`, now plays on the explicit Transmit regardless of the ambient **Sound** toggle — Sound only mutes the typing bleeps); **cancels** on re-press / text edit (`onInput`) / voice-switch; **safety timer** backstops names mode if `onend` never fires; iOS-safe (fires on tap → `unlockAudio`). The `.speaking` glyph class = bright-white + drop-shadow.
  - *Possible next polish: a true radio-filtered voice (can't bandpass Web Speech output through Web Audio — would need a different TTS path); tune droid timbre/pacing; maybe a 3rd "alien syllable" conlang voice.*

### New this round (038 Pottery Wheel) — PUSHED `main 0f331b2`, 38 toys live (`.claude/settings.json` left out as always)
- **038 Pottery Wheel** (`toys/pottery-wheel/`, category `wellness`) — a calming **pseudo-3D** pottery wheel in plain Canvas 2D (no WebGL/build — see the 3D decision below). A lump of clay spins on a wheel head; **drag up & down the clay's side to pull the walls out/in** (`shapeAt` maps y→row, `|x−cx|`→target radius, Gaussian finger brush `BRUSH=12`, `SHAPE_RATE=0.45`). Clay = **surface of revolution**: `ROWS=150` per-row radii eased toward `tgt` + 2-pass smoothing; rendered as stacked shaded horizontal bands **clipped to a smooth vessel silhouette path** (kills band-edge stair-stepping) with a per-row horizontal cylinder gradient, an occlusion veil toward the base, a rounded **foot** ellipse seating it into the wheel, a hollow **rim opening** (dark radial interior + lit lip arc), and a rotating throwing-mark. Wheel head = ellipse + spinning radial ticks (`SPIN_SPEED=3.4`). **Shading is the key fix:** a hue-preserving 3-point clay ramp `CLAY_SHADOW[78,42,28]→CLAY[196,120,80]→CLAY_LIT[233,181,134]` (never white) + only a faint matte sheen `pow(diff,3)*0.10` — the earlier `b=0.30+0.92*diff` + tight `pow(diff,7)*0.55` spec made it read **polished copper/metal**; now it's matte **terracotta**. **Audio (synth, owner rule no samples):** low triangle/sine wheel-motor hum (68/136 Hz through 280 lowpass + 5.2 Hz LFO) + a bandpassed-noise **wet-clay shaping bed** gated by how fast the wall is moving (`shapeGain` follows `moved`). Sound on/off toggle + iOS unlock. **New clay** resets to a rounded cylinder. Tunables at top of `script.js`. Toy assets `script.js?v=1`/`styles.css?v=1`.
- **Fire it / kiln reveal (owner: "I'd like an option to fire it and see what I made")** — added a **Fire it** button (warm accent, `.ctrl-btn--fire`). Press → wheel slows, the pot **glows incandescent in the kiln** (a `heat` 0→1 temperature drives an additive `incand()` colour ramp dull-red→orange→yellow-white over the vessel + a soft kiln halo bleeding into the scene), then **cools to reveal a finished GLAZED piece** — a random glaze from `GLAZES[]` (Celadon/Cobalt/Oxblood/Honey/Matte White/Sage Ash-speckled), its name shown in a fading caption beneath. Timeline `HEAT_UP 2.0 / SOAK 0.9 / COOL 2.8` s; during cool the material crossfades clay→glaze (`matMix`, `lerpMat`). **Shading is now material-driven** (`buildShade(mat)` takes shadow/base/lit + `gloss`): matte clay rides the colour ramp; glossy glazes add a **crisp narrow specular glint** (`specP=3+gloss*11`, `specAmt=0.10+gloss*0.30` — earlier `+gloss*0.5` blew the glaze white). Shaping is locked once firing/fired; **New clay** returns to wet clay + re-enables Fire. **Audio:** `kilnRoar()` (swelling filtered-noise band, breathing LFO) during firing + a soft two-tone `chime()` on the reveal. **Cursor fix (owner: "why do I have a hand cursor after I fire it?"):** the canvas had a hardcoded CSS `cursor:grab`; the script now toggles an `is-done` class (cursor → `default`) while firing, and `is-paint` (cursor → `crosshair`) while painting; removed on New clay.
- **Painting phase after firing (owner: "it might be nice to have a painting phase after firing")** — when the kiln reveal completes, `enterPainting()` switches `mode` from firing → **`painting`** (there's no separate idle "fired" state now): a **7-swatch colour palette** (`#palette`, JS-built from `PAINTS[]` Ink/White/Cobalt/Gold/Coral/Sage/Plum) fades in bottom-centre and you **drag on the pot to decorate it**. Paint = **wrap-around marks**: `paintAt()` maps screen-x→surface angle (`relA=asin((x−cx)/rad)`, absolute `a=relA+spin`) and stores `{a,r,c,rad}` in a `MAX_MARKS=1500` ring buffer; `drawPaint()` (inside the silhouette clip) draws each as an ellipse only when on the front half (`cos(a−spin)>0.05`), x-squished + alpha-faded toward the edges and brightness-modulated by the same key light, so marks **sweep to the front, round to the back, and return** as the wheel turns — real 3D decoration. Because the fired wheel keeps gently turning (painting spin `SPIN_SPEED*0.34`, faster than firing's `*0.16` so a held brush bands in ~5s), **holding the brush lays a band** (authentic banding-wheel) and deliberate strokes make motifs; drag is interpolated so fast strokes stay solid. New clay clears `marks` + hides the palette + restores the throw hint. Toy now `script.js?v=4`/`styles.css?v=4`. Card still the fired Celadon (un-painted) — fine; a painted card is a possible future swap (headless rAF throttling makes banding hard to capture cleanly).
- Registered everywhere: tools-registry.json (prepended, newest-first; shortDescription mentions firing), sitemap.xml, TYPE_NL_PHRASES (main.js; +kiln/fire/glaze/celadon/cobalt/oxblood), card rule + `:not()` chain (styles.css). **Real rendered card** `assets/cards/pottery-wheel.png` = a **fired Celadon vase grounded on the wheel** (re-shot after owner: the first terracotta-bust crop "looks like an avatar" — green glaze + visible wheel disk + rim opening now read unmistakably as pottery) + OG `assets/og/pottery-wheel.png` (og-gen `#pottery-wheel`, accent `#e9b586`, motif auto-uses the card, desc mentions firing). Hub cache-bust bumped **`styles.css?v=40`** + **`main.js?v=38`** (index.html + all-toys.html). Verified headless: registry JSON parses, no console errors, no horizontal overflow @375px, 3 controls fit @375px, fire disables while firing + New clay re-enables, touch works. Home featured spotlight auto-switches to Pottery Wheel (newest registry entry).
- **3D direction decision (owner agreed):** stay **vanilla Canvas-2D pseudo-3D by default** (fixed viewpoint / surface-of-revolution / relief / parallax) to keep the zero-build, single-file, hand-tweakable repo soul; reserve **single-file raw WebGL** (no Three.js, no build) as the escape hatch for the rare toy that genuinely needs a free-orbit camera or volumetric depth; avoid Three.js unless we ever do a whole cluster of real-3D toys (only then does the ~600KB dep + build amortize).

### New this round (033–034) — BUILT, awaiting push
- **033 Fireworks** (`toys/fireworks/`, visual) — tap the night sky to launch shells that arc up (`vy:-(WATER-ty)/0.72`, gravity `220 px/s²`) and **burst** into peony/ring/willow/palm/crackle/chrysanthemum sparks (physics in clean px/s units: spark `g≈130`, `drag=0.4` via `Math.pow(0.4,dt)`, burst radius `B=Math.min(W,H)*0.52`). Additive trail buffer (`tcv` faded `destination-out rgba(0,0,0,0.18)`/frame, sparks drawn `lighter`); **water reflection** (flipped clipped `drawImage(tcv)` below `WATER=H*0.84`, α0.32); rockets/sparks/smoke; auto-launch ambient. **Audio (synth):** whistle (rising bandpass noise) + boom (sine 80–120→40 + highpass crack) + crackle (26 random filtered pops). Spark color via `lum=46+20*k`. ⚠ Early bug (self-caught): a `*30` position scaling fought gravity → faint fountain; fixed by px/s rewrite.
- **034 Rain on a Window** (`toys/rain-window/`, wellness) — a foggy pane over a warm **bokeh dusk** (full-height bokeh + warm city-glow from below + cool sky-glow from above; base `#46607f`→`#636a6e`). KEY model: **the fog is a BLURRED, frosted copy of the scene** (`frost` offscreen = `blur(maxWH*0.018)` of `bg` + `rgba(214,225,238,0.2)` veil), so wiping (`destination-out`) reveals the **SHARP** scene at the same brightness — reads as "clearing/sharpening", never a dark smear (the earlier flat light-gray fog made wipes look like dirty smudges). Re-mist = redraw `frost` at low α. **Drops:** `cond[]` = fine condensation "food" (continuously re-forms `W*H/2400·dt`/s); `drops[]` cling and **grow by absorbing cond + merging with smaller drops**, and only **slide once `r≥SLIDE_R=8.5`** (gravity beats surface tension), with **gentle** wobble (`amp 0.15+r*0.012`, `wobSpeed 1.6–2.6` — was way too wavy before). **Re-fog glass** control button (`#fogBtn`→`refog()`). **Audio (synth):** rain noise beds + distant thunder + drip ticks. ⚠ All 3 owner fixes applied this round: re-fog button, calmer wobble, cling-grow-merge-then-slide.
- Both: rendered card thumbnails (`assets/cards/fireworks.png` from a multi-burst moment; `assets/cards/rain-window.png` from a wiped reveal) + per-toy OG (`assets/og/`). Registered in tools-registry.json (033 visual / 034 wellness, prepended newest-first), sitemap.xml, TYPE_NL_PHRASES (main.js), card rule + :not() chain (styles.css). Cache-bust hub `styles.css?v=34` + `main.js?v=32`. (Toy-local assets still `?v=1` — fine, never deployed.) **all-toys sorts alphabetically**, so rain-window appears on a later infinite-scroll page, fireworks on page 1 — both confirmed registered.

### New this round (031–032, wellness)
- **031 Aurora Drift** (`toys/aurora-drift/`, wellness) — drag paints soft draped **aurora curtains** (green/teal/violet ray particles on a travelling-wave sway, additive `lighter`, fade in/out; `MAX_RAYS=150`, `sat=78`) over 2 layered near-black snowy-peak silhouettes (`#0a1020`/`#02040a`) with faint rim light + starfield; a gentle self-running ambient aurora keeps it alive on arrival. Audio = very soft evolving sine pad (A2/E3/A3/B3 + low LFOs) + breathy bandpass air that opens while painting + sparse pentatonic `shimmer()`. ⚠ Tuning history: first pass was far too intense (neon flames, blown to white, peaks invisible) — fixed by dropping ray count/alpha/sat, calming amp, and making peaks solid near-black silhouettes w/ rim light.
- **032 Campfire** (`toys/campfire/`, wellness) — tap tosses a **log** (flares + grows fire), drag **fans** flames; particle flames (white-hot→orange→red tongues, vertically elongated)/embers/smoke, warm ground glow, log + rock ring. **Logs burn away:** each has `fuel` (1→0, ~40-50s, faster when fanned), chars brown→charcoal + shrinks + fades, removed at fuel≤0; fire `intensity` eases toward `FLOOR+totalFuel*0.5` (FLOOR=0.34) so it's bright with logs, sinks to an ember floor as they char, and spikes when you add one. **Audio fully synthesized (owner requirement, no samples):** filtered-noise roar bed w/ breathing LFO + Poisson crackle pops (mostly faint ticks, ~12% sharp broadband woody snaps — NO tonal ring, that was the "ping") + high sizzle + log-add = woody resonant-mode knock (2 hits) + bark scrape + spark flare. Owner to audition audio + watch burn-down live.
- Both: rendered card thumbnails (`assets/cards/`) + per-toy OG (`assets/og/` via og-gen.html). Registered in tools-registry.json (prepended), sitemap.xml, TYPE_NL_PHRASES (main.js), card rule + :not() chain (styles.css). Cache-bust hub `styles.css?v=33` + `main.js?v=31`. Wellness filter now shows 5 (Aurora Drift, Breathing Pacer, Campfire, Floating Lanterns, Zen Sand Garden).

### Prior round (029–030, wellness)
- **029 Zen Sand Garden** (`toys/zen-sand-garden/`, wellness) — drag rakes lit, combed furrows into a **persistent offscreen sand layer** (per-tine: shadow valley + highlight ridge offset toward a top-left light); tap sets a smooth lit pebble (irregular blob outline, radial gradient, contact shadow) with concentric rings raked around it. "Smooth sand" (clears furrows, keeps stones) / "Clear stones" controls. Audio = speed-driven continuous raking noise (bandpassed brown noise) + wooden "tock" (sine thunk + click) on placement. Tap-vs-drag: a near-stationary quick press (<8px, <360ms) places a stone; movement rakes.
- **030 Floating Lanterns** (`toys/floating-lanterns/`, wellness) — night lake scene (twinkling stars, moon + glow + shimmering reflection path, 2 layered hill silhouettes, water band at HY=0.72H); tap releases a warm glowing **sky lantern** (rounded paper capsule + halo + flame core, composite `lighter`) that rises/sways/flickers/shrinks/fades, **mirrored on the lake** (clipped to water, flipped+squashed, alpha fades as it climbs). Audio = ambient night wind (low brown noise + slow LFO) + lake lapping (2nd lower band) + soft pentatonic shimmer + airy "fwoom" on release.
- Both: rendered card thumbnails (`assets/cards/`) + per-toy OG (`assets/og/` via og-gen.html). Registered in tools-registry.json (prepended), sitemap.xml, TYPE_NL_PHRASES (main.js), card rule + :not() chain (styles.css). Cache-bust hub `styles.css?v=32` + `main.js?v=30`.

### Earlier this session (025–028)
- **025 Kaleidoscope** (`toys/kaleidoscope/`, visual) — drag to stir glowing jewel shards into 12-fold mirrored symmetry, tap to drop a gem. Offscreen "object cell" reflected through dihedral wedges.
- **026 Zen Ripple Pond** (`toys/ripple-pond/`, visual) — real damped-wave height-field sim (9-point Laplacian to avoid checkerboard instability) rendered as moonlit water; koi (proper fish shapes w/ koi patterns), lily pads + lotus. **Audio:** rock-drop *ker-plunk* (impact thud + rising cavity bloop + splash noise) + water-relevant ambient (layered brown-noise river bed w/ moving lowpass + sweeping bandpass + occasional panned trickle runs). NO pure drone.
- **027 Wind Chimes** (`toys/wind-chimes/`, audio) — **rigid support bar that swings (phi) AND tilts (psi)**; tubes are pendulums driven by their **moving pivots** (correct `g/L` gravity + pivot-acceleration coupling — earlier versions had ~100× too-weak stiffness = "zero-G", since fixed). Grab the **bar** (move whole thing), a single tube, or the clapper; tap to ring. Tone = bright upper-octave pentatonic w/ **inharmonic tubular-bell partials** (ratios ~1, 2.76, 5.40, 8.93) + mallet "tink" transient + ~8.5k lowpass.
- **028 Marble Drop** (`toys/marble-drop/`, game) — plinko: glossy glass marbles bounce down a brass pegboard into bins (Galton bell curve). Anti-rest peg nudge + a per-frame **anti-stuck** pass (wall-biased) so marbles never lodge in the field or on the side rails. **Auto** button = self-running sim; Clear button. Glass-clack audio (velocity-scaled, throttled).
- **all-toys.html:** **infinite scroll** (12/page via an IntersectionObserver sentinel after `#toolsGrid`, in `assets/main.js` `renderNextPage`/`ensureScrollObserver`; home's random-9 path untouched) + the **Friends of the gallery** `<aside>` (copied from index.html, above the footer).
- **Audio rule (now in project DECISIONS.md):** realistic, physically-grounded synthesis across every toy with sound — transient + body + tail, velocity-scaled, consonant. Headless can't audition sound, so audio is verified structurally only — owner should listen + tune.
- Shared-asset cache-bust (025–028 round): `styles.css?v=31`, `main.js?v=29` (now superseded by v=32 / v=30 above).

## ⚠ Design quality bar — READ BEFORE BUILDING ANY TOY
The owner is a **designer** and holds a high visual bar. A toy is NOT done when it merely "works" — it must look *intentional, polished, and richly itself*. The first desktop pass at toys 020–023 was rejected for being flat/crude with unrepresentative cards; this is the standard that pass missed:
- **Every toy MUST be interactive** (owner rule, 2026-06-22). No purely passive "just watch" toys — each must respond to tap / drag / click / controls in a meaningful way (e.g. Lava Lamp: drag to stir the wax; Spirograph: gear sliders; Plasma Ball: arcs follow your finger). If a concept is inherently ambient, add a way to disturb, shape, or steer it.
- **Each toy is its own curated little world** — a deliberate palette, lighting, depth, motion. No flat primitives on flat backgrounds. Study the strongest live toys before building and match that level: **Goo Cursor, Star Click Sky, Tiny Idle Garden, Falling Sand, Three Doors, Plasma Ball**.
- **Render real-world toys *as the real object*** — material, lighting, shadow, glow. (Newton's cradle needs a grounded frame + chrome balls, not flat dots. A spirograph needs dense layered glowing rosettes, not one thin loop. Plasma needs jagged crackling lightning, not smooth rotating lines.) [Note: Lava Lamp is intentionally full-viewport metaballs now — NOT a vessel — per owner request; don't re-add a lamp shell.]
- **Card images MUST clearly represent the toy.** Strongly prefer a **real rendered thumbnail** captured from the finished toy (`assets/cards/<slug>.png`, overlays hidden, square-ish crop) over abstract CSS gradients — abstract blobs that don't read as the toy are NOT acceptable. The card CSS is then `background:#<bg> url("cards/<slug>.png") center/cover`.
- **Never ship without an OG image.** Generate one for every toy via `scripts/og-gen.html` (you can reuse the rendered card thumbnail as the motif — see the `img()` helper + toy entries already in that file). A missing `assets/og/<slug>.png` = a broken share link.
- **Always SCREENSHOT and look at it** (toy, card, and OG) before committing — "no console errors" is not the bar; *how it looks* is. Note: headless Chromium throttles `requestAnimationFrame`, so animated toys may not finish drawing in a normal screenshot — drive frames with `await page.evaluate(()=>new Promise(r=>requestAnimationFrame(r)))` in a loop, or the toy will look unfinished when it's actually fine.

## ⚠ Audio quality bar — READ BEFORE BUILDING ANY TOY WITH SOUND
Sound is a **first-class part of the presentation**, held to the same standard as the visuals (owner directive, 2026-06-30). A toy's audio isn't done when it plays the right note — it must sound **rich, spacious, and intentional**. Full standard + reference recipes in `.ai/memory/DECISIONS.md` ("Audio quality bar"). The essentials:
- **Layered, physically-grounded voice:** `transient + body (correct harmonic/inharmonic partials) + decaying tail`. Know the physics — clamped-free bar (kalimba) is inharmonic ≈1:6.27:17.55; a tuned tongue drum/hang is harmonic (strong octave ×2, twelfth ×3); a tube (chimes) ≈1:2.76:5.40. Right partial ratios = "real instrument" vs "beep."
- **Space is non-negotiable:** convolver reverb (smooth impulse — low-pass the noise so the tail isn't grainy; highshelf for shimmer, highpass so lows stay clean) + a feedback **delay** where it fits (feed it into the reverb so repeats bloom). Dry = cheap.
- **Stereo width** (pan voices by on-screen position), **bus compressor** (glue + anti-clip on strums), master lowpass for silk, **velocity/detune variation** (no machine-gun sameness), **consonant scales** (pentatonic/Akebono), **iOS unlock** + Sound toggle.
- **Headless CANNOT hear** — the automated pass only proves the graph builds. Audio is always "needs the owner's ears"; say so and iterate on feedback. Copy **Kalimba** / **Steel Tongue Drum** audio architecture as the starting point.
- **Meta-principle:** *keep pushing the presentation envelope — visual AND audio — as far as we dare within reason.* Each release a notch more polished than the last; when touching a toy, look for the upgrade, not just "works" (without breaking the zero-build vanilla soul).

## To continue on another machine
1. `git pull` (the remote is authoritative; everything below is committed/pushed).
2. Run locally — **no build, no deps needed**: from the repo root, `python3 -m http.server 8000`, then open `http://localhost:8000`.
3. Only if you need the headless verify / screenshot / OG-render workflow (Playwright): `npm install` then `npx playwright install chromium`. **`node_modules` is gitignored on purpose** (it once bloated pushes), so it won't come down with the pull — reinstall it.

## What this site is now (post-pivot, 2026-06)
A branded **launcher hub** + **19 standalone, full-bleed toys**, each in its own `toys/<slug>/` (or `tools/<slug>/` for the lone utility), opening in a NEW TAB. Geist design system (Geist Sans/Mono, neutral grays, restrained red `#941e1e`, 3-way System/Light/Dark theme). The old ~57 toys were archived to `/archive/`. **Direction: FUN / playful / experiential — NOT dev tools** (dev tools belong on the separate BuildUtilities site). Static site, GitHub Pages from `main` → onepagetoys.com.

**The 39 toys:** Goo Cursor, Meeting Cost Meter (the one utility), Breathing Pacer, Coin Flip, Dice Roller, Magic 8-Ball, Blob Choir, Tic-Tac-Toe, Rock Paper Scissors, Three Doors, Memory Match, Snake, Weird Generative Canvas, Star Click Sky, Tiny Idle Garden (015), Echo (016), Chimp Test (017), Beat Maker (018), Falling Sand (019), Lava Lamp (020), Bubble Wrap (021), Newton's Cradle (022), Spirograph (023), Plasma Ball (024), Kaleidoscope (025), Zen Ripple Pond (026), Wind Chimes (027), Marble Drop (028), Zen Sand Garden (029), Floating Lanterns (030), Aurora Drift (031), Campfire (032), Fireworks (033), Rain on a Window (034), Perfect Circle (035), Pin Art (036), Chladni Plate (037), Pottery Wheel (038), Aurebesh Translator (039).

## Key files
- `tools-registry.json` — drives the gallery. Prepend new toys (newest first). slug/name/shortDescription/category/tags/status/path.
- `assets/main.js` — gallery render + search; `TYPE_NL_PHRASES` map (natural-language search terms per tag); home shows a RANDOM 9; GA4 events (`toy_launch`, `outbound_click`, `tip_jar_click`). Cache-bust `?v=N`.
- `assets/styles.css` — hub styles + per-slug `.card__preview[data-slug="…"]` thumbnails (+ a `:not()` default-exclusion list). Cache-bust `?v=N`.
- `assets/theme.js` — 3-way theme toggle. `assets/tip-jar.js` — Mighty Army shield badge → PayPal `brent@mightyarmy.com`.
- `sitemap.xml`, `assets/og/<slug>.png` (per-toy share images — every toy has one), `assets/cards/<slug>.png` (real rendered card thumbnails — used by Falling Sand + Lava Lamp + Bubble Wrap + Newton's Cradle + Spirograph; the rest use CSS motifs, but new toys should prefer a rendered thumbnail per the quality bar above).
- **`scripts/og-gen.html`** — parameterized 1200×630 template for per-toy OG share images (and the Falling Sand card thumbnail). Open `file://…/scripts/og-gen.html#<slug>` and screenshot at 1200×630. (Was historically in `/tmp` — now committed here.)

## Conventions / how to add a toy
- New `toys/<slug>/` = self-contained `index.html` + `styles.css` + `script.js`, full-bleed, frame corners (`No. NNN`, name, back-link), a hint line, includes `assets/tip-jar.js?v=9`. Full SEO meta + JSON-LD + GA snippet + no-flash theme init (copy any recent toy as a template — e.g. `toys/echo/` or `toys/falling-sand/`).
- Register: add to `tools-registry.json` (top) + `sitemap.xml` + `TYPE_NL_PHRASES` in `main.js` + a `.card__preview[data-slug]` rule in `assets/styles.css` (and add the slug to the default `:not()` chain) + a per-toy OG image via `scripts/og-gen.html`.
- **Cache-busting:** bump `?v=N` on any shared asset you change (`assets/styles.css`, `assets/main.js`, `assets/theme.js`, `assets/tip-jar.js`) across `index.html` + `all-toys.html`, and on a toy's own `script.js`/`styles.css` in its `index.html`. Browsers cache aggressively.
- **Audio toys (LEARNED):** always add the iOS silent-buffer unlock + `resume()` inside the first user gesture, or Web Audio can stay silent on iOS Safari — `var b=actx.createBuffer(1,1,22050); var s=actx.createBufferSource(); s.buffer=b; s.connect(actx.destination); s.start(0);`. Tune tones to be consonant + calm (pentatonic / major-6, lower octaves, sine + lowpass).
- **3D-transformed elements (LEARNED):** on a `transform-style: preserve-3d` element, `box-shadow` rings render with a clipped edge — use `outline` for focus/selection rings instead (bit Three Doors' picked door).
- Verify each toy headless before committing (Playwright): no console errors, no horizontal overflow at 375px, interactions work. Screenshot to eyeball visuals.

## Open ideas / next candidates (fun-aligned)
Toys 020–023 built, redesigned to the quality bar, and given real card + OG images. Next candidates: archive options **Mood Meteor**, **Void Oracle**, **Emoji Slots**; genre gaps: more audio, more cozy/idle. Skip as too-simple: the reflex cluster + one-shot gag generators in `/archive/`.

## Other repos (separate)
- **BuildUtilities** (`~/Personal Projects/buildutilities` = GitHub `brentspore/buildutilities` = Lovable project "BuildUtilities.com") — the dev-tools site; git push syncs into Lovable, then Publish in Lovable to deploy. Different repo entirely.
